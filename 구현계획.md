# 온중(溫證) 구현 격차 분석 및 단계별 실행 계획

> 작성 기준일: 2026-08-23  
> 기준 문서: `상세서.md` v1.0  
> 분석 대상: 현재 워크스페이스의 소스, 설정, 쉼터 원천 데이터  
> 권장 구현 경로: 기존 TanStack Start 프로젝트를 유지하고 기능 명세를 이식한다.

## 최신 실행 현황 — 2026-08-24

아래 `0. 최초 분석 결론`과 Phase 체크리스트는 2026-08-23에 작성한 구현 기준선이다. 체크박스는
실시간 완료 대장이 아니므로 현재 상태 판단에는 이 절을 우선한다.

- Phase 0~8의 애플리케이션 코드, 외부 API 어댑터, Supabase 마이그레이션, RLS 계약,
  보존 worker, 테스트 및 Vercel 빌드 경계는 로컬 구현을 완료했다. 2026-08-24 후속
  안정화에서 홈 실데이터 최신성·timeout, 약품 후보 단위 원자적 MFDS 보강, 대상자 등록
  멱등성, 기존 Auth 사용자 ADMIN 승격 방지를 추가했다.
- Vitest 192개 파일·1,251개 테스트를 통과했다. 기본 실행에서 제외되는 실 API smoke
  5개는 `test:live-apis`로 별도 실행해 KMA·Supabase·Gemini·MFDS·Naver·TMAP 실계정
  호출을 모두 통과했다.
  TypeScript, ESLint(오류 0, 기존 Fast Refresh 경고 7), 프로덕션 Vercel 빌드와
  비밀정보 스캔을 통과했다.
- Playwright는 로컬 실행 가능한 12개 시나리오를 통과했다. 실계정 API, Base Sepolia,
  외부 알림 공급자가 필요한 3개 시나리오는 출시 환경에서 실행하도록 명시적으로 제외했다.
- Docker/PostgreSQL 부재로 실제 migration reset·pgTAP·schema diff·Supabase 타입 생성을
  실행하지 못했다. 원격 Supabase에는 `register_subject_service_role`,
  `replace_medication_review_candidate`, `subject_registration_receipts`가 아직 없으며 현재
  CLI 계정은 원격 migration 권한이 없다. Vercel Preview, Base Sepolia schema 등록,
  실 API 자격증명 smoke, SMS/알림톡 공급자 연결도 외부 출시 게이트로 남아 있다.
- 현재 폴더에는 `.git`이 없으므로 Lovable 원격 이력을 보존한 정상 clone 확인이 필요하다.

따라서 **현재 로컬 코딩 단계는 완료**, **외부 인프라를 포함한 운영 출시 인수는 미완료**다.
원격 적용 순서는 `호환 앱 배포·구 인스턴스 종료 → Owner/Admin CLI로 migration 적용 →
첫 ADMIN bootstrap → 실제 동의 대상자 등록`으로 고정한다. 운영 프로젝트에는 로컬 seed의
가상 계정·대상자를 적용하지 않는다.

## 0. 최초 분석 결론 — 2026-08-23 기준

현재 프로젝트는 **완성된 서비스가 아니라 HRI 계산식과 S-01 대시보드 외형을 보여주는 목데이터 기반 프로토타입**이다.

- F-01 HRI 수식과 등급 판정은 구현되어 있다. 실제 기상·복약·체크인 데이터, 배치 계산, DB 저장, 단위 테스트는 없다.
- F-02 약봉투 판독, F-04 그늘길, F-05 크라우드소싱·온체인은 구현되지 않았다.
- 명세의 7개 화면 중 S-01만 부분 구현됐다. S-02, S-03, S-05, S-06, S-07, S-08 라우트는 없다.
- Supabase, PostGIS, 기상청, 식약처, Gemini, Naver Maps, TMAP, EAS, Vercel Cron 연동은 없다.
- 쉼터 원천 파일은 950건을 정확히 포함한다. 그러나 명세가 요구하는 GeoJSON, 시드 스크립트, 시설 유형, `is_im_bank`, KMA 격자 값은 아직 생성되지 않았다.
- 현재 워크스페이스에는 로컬 `node_modules`가 없어 빌드·린트·타입 검사를 정상 완료하지 못했다.

기능별 판정은 다음과 같다.

| 기능             | 판정      | 현재 증거                          | 완료까지 필요한 핵심 작업                                           |
| ---------------- | --------- | ---------------------------------- | ------------------------------------------------------------------- |
| F-01 HRI         | 부분 구현 | `src/lib/risk/hri.ts`              | 테스트, 실제 입력, 캐시, 배치, 스냅샷, 알림 전이                    |
| F-02 약봉투 판독 | 미구현    | 목 약품 데이터만 존재              | 이미지 업로드, Gemini, 식약처 3종, 13계열 매칭, 수동 폴백, S-03     |
| F-04 그늘길      | 미구현    | CSS로 그린 `MiniMap`만 존재        | Naver 지도·TMAP 보행 경로, 건물·휴식·경사 데이터, 그림자 계산, S-05 |
| F-05 제보·온체인 | 미구현    | 목 EAS UID와 배지만 존재           | S-06, EAS 스키마·기록·재시도, 체크인 분리, S-07                     |
| 공통 UI          | 부분 구현 | 토큰, Badge, Button, Ribbon, Shell | Input, Table, Modal, Toast, Map, 상태 UI, 접근성 보완               |
| 데이터·보안      | 미구현    | SQL은 설계서에만 존재              | Supabase 마이그레이션, RLS, Auth, Storage, 타입 생성                |

## 1. 분석 범위와 근거

### 1.1 확인한 현재 구조

핵심 파일은 다음과 같다.

| 경로                       | 현재 역할                              | 판정                                            |
| -------------------------- | -------------------------------------- | ----------------------------------------------- |
| `src/lib/risk/hri.ts`      | HRI 순수 계산 함수와 열거형            | 재사용 가능, 이유 문구 계산 보완 필요           |
| `src/lib/mock/data.ts`     | 대상자 12명, 쉼터 6곳, 돌봄 이벤트 4건 | 전부 데모 데이터. 운영 코드에서 제거 필요       |
| `src/routes/index.tsx`     | 데모 진입 화면                         | 명세 외 보조 화면. 실제 상태 요약으로 교체 가능 |
| `src/routes/dashboard.tsx` | S-01 외형                              | 부분 구현. DB·지도·URL 상태·실제 폴링 없음      |
| `src/components/onjung/*`  | 온중 전용 Badge, Button, Ribbon, Shell | 기본 자산으로 유지하고 규격 보완                |
| `src/styles.css`           | Paper/Shade 토큰과 타입 스케일         | 상당 부분 구현. 폰트·시니어·safe-area 보완      |
| `src/routeTree.gen.ts`     | 생성된 라우트 트리                     | 실제 라우트는 `/`, `/dashboard` 두 개뿐         |
| `package.json`             | TanStack Start/Vite/React 19           | 외부 연동·테스트 의존성 없음                    |
| `무더위쉼터/*`             | SHP/DBF/PRJ/SHX/CPG 원천 데이터        | 950건 확인. 전처리 산출물 없음                  |

### 1.2 현재 구현에서 확인한 문제

1. `src/routes/index.tsx`, `src/routes/dashboard.tsx`, `src/components/onjung/Shells.tsx`는 존재하지 않는 `/subjects/$id`, `/shelters`, `/verify/$uid`, `/alert/$eventId`로 이동한다.
2. S-01의 30초 갱신은 네트워크 요청이 아니라 `setInterval()`로 `refreshing` 문구만 바꾼다.
3. `MiniMap`은 Naver Map이 아니라 절대 위치의 `<span>`으로 마커처럼 보이게 만든 박스다.
4. 필터 상태는 React 로컬 상태에만 있으며 `?gu=&level=&sort=` URL 규칙을 따르지 않는다.
5. `<Link><Btn /></Link>` 구조는 링크 안에 버튼을 중첩한다. 링크형 버튼을 `asChild` 또는 링크 전용 스타일로 바꿔야 한다.
6. `src/routes/__root.tsx`는 `lang="en"`, 일반 viewport, `Lovable App` 메타데이터를 사용한다.
7. 전역 404·500 문구는 영어이며 명세의 오류 작성 원칙과 맞지 않는다.
8. 시니어 모드는 `localStorage`만 사용한다. 서버 프로필 우선 규칙, 60px 버튼, 28px 아이콘이 없다.
9. `src/lib/risk/hri.ts`의 환경 이유 문구는 열대야를 포함한 전체 E 값을 `체감온도 + 특보` 점수로 표시한 뒤 열대야 이유를 따로 추가한다. 예를 들어 E가 상한 50이면 `체감 + 경보 (+50)`로 표시돼 명세 예시 `(+47)`과 달라진다.
10. 테스트 스크립트와 테스트 파일이 없다. HRI 경계값, 중복 약물 계열, 상한·하한, 체크인 차감이 자동 검증되지 않는다.

### 1.3 쉼터 데이터 확인 결과

`무더위쉼터.dbf`를 직접 검사한 결과는 다음과 같다.

| 항목                             | 확인값 |
| -------------------------------- | -----: |
| 전체 레코드                      |    950 |
| `geoIdn` 고유값                  |    950 |
| 지오코딩 성공(`resultType=SUCC`) |    950 |
| 경로당                           |    466 |
| 금융기관                         |    245 |
| 행정복지센터                     |    129 |
| 기타                             |    110 |
| iM뱅크/대구은행 이름 매칭        |    100 |

분류 규칙은 아래 조건을 위에서부터 적용하면 설계서의 집계와 정확히 일치한다.

```ts
if (/경로당/.test(name)) facilityType = "경로당";
else if (/행정복지센터|주민센터/.test(name)) facilityType = "행정복지센터";
else if (/은행|농협|신협|새마을금고/.test(name)) facilityType = "금융기관";
else facilityType = "기타";

isImBank = /iM|아이엠|DGB|대구은행/.test(name);
```

- DBF의 `x`, `y` 속성은 WGS84 경도·위도다.
- SHP 도형은 `Korea_2000_East_Belt_2010` 투영 좌표다.
- 시드 생성은 DBF의 검증된 `x`, `y`를 사용하거나 SHP를 WGS84로 재투영해야 한다. 두 좌표계를 섞으면 안 된다.
- 구·군은 도로명주소의 `대구광역시 {구·군}`에서 파싱할 수 있으며 8개 구·군 모두 누락 없이 확인됐다.
- `geoIdn`을 정렬한 뒤 `DG-${geoIdn.padStart(4, '0')}`로 변환하면 안정적인 쉼터 ID를 만들 수 있다.

### 1.4 실행한 검증

| 검증                 | 결과                                  | 해석                                                           |
| -------------------- | ------------------------------------- | -------------------------------------------------------------- |
| HRI 경계값 직접 실행 | 31/33/35/38/40℃ 구간과 등급 계산 일치 | 순수 계산식은 재사용 가능                                      |
| `npm run build`      | 실패                                  | 로컬 의존성 없음. 상위 경로의 Vite 임시 파일 쓰기 `EPERM` 발생 |
| `npm run lint`       | 실패                                  | 프로젝트 로컬 ESLint 실행 파일 없음                            |
| `npx tsc --noEmit`   | 실패                                  | 로컬 의존성이 없어 모듈 타입을 찾지 못함                       |
| Git 상태             | 확인 불가                             | 현재 폴더에 `.git` 저장소가 없음                               |

이 결과는 소스가 반드시 깨졌다는 뜻이 아니다. 먼저 프로젝트 루트에 잠금파일 기준 의존성을 설치한 뒤 동일 검증을 다시 실행해야 한다.

### 1.5 적용한 스킬

요청한 `find-skills`로 `code-maturity-assessor`, `gap-analysis`, `software-architecture`, `react-code-review` 계열을 검색했다. 외부 스킬을 새로 설치하지 않고 다음 설치済 스킬을 적용했다.

- `software-architecture`: 도메인 로직, 외부 연동, UI를 분리하고 파일 책임과 의존 방향을 정했다.
- `concise-planning`: 작업을 선행조건, 원자적 작업, 검증, 완료조건 순으로 정리했다.
- `supabase`: RLS, 최소 권한, 마이그레이션, 타입 생성, Storage 보안 원칙을 DB phase에 반영했다.
- `writing-clearly-and-concisely`: 작업자가 그대로 실행할 수 있도록 동사형 체크리스트와 구체적 파일명을 사용했다.

## 2. 설계서에서 구현 전에 확정해야 할 사항

아래 항목은 코드 누락이 아니라 **설계서 자체의 충돌 또는 최신 외부 환경과의 불일치**다. Phase 0에서 먼저 고쳐야 이후 구현을 되돌리지 않는다.

### 2.1 프레임워크 불일치

- 설계서: Next.js 15 App Router
- 현재 프로젝트: TanStack Start 1.168 + Vite 8 + React 19

**결정:** 기존 TanStack Start를 유지한다. 기능·URL·SSR·보안 요구사항은 그대로 구현하고 `상세서.md` §2.1·§2.2의 프레임워크와 디렉터리 예시만 현재 프로젝트에 맞춘다.

이유는 현재 라우팅, SSR 오류 처리, Lovable 설정, UI 코드가 TanStack Start에 결합돼 있기 때문이다. Next.js로 옮기면 기능 구현과 무관한 전면 재작성 비용이 발생한다. 대회가 Next.js 자체를 요구하는 경우에만 Phase 0에서 별도 마이그레이션을 수행한다.

### 2.2 기상청 1차 데이터 소스 종료

설계서의 1차 소스인 `대상환경별 여름철 체감온도` 서비스는 2026-05-01 종료됐다. 현재 생활기상지수 4.0 목록에도 해당 엔드포인트가 없다.

**결정:** 환경 입력의 우선순위를 다음처럼 바꾼다.

1. 기상청 API허브 고해상도 500m 격자의 일반 체감온도 `ta_chi`를 사용할 수 있는지 계정 권한과 응답을 통합 테스트한다.
2. 사용할 수 없으면 기상청 초단기실황/단기예보의 `TMP`·`REH`로 Stull 근사값을 계산한다.
3. 두 소스가 모두 실패하면 3시간 이내 마지막 유효 E를 유지한다.
4. 마지막 유효값도 없으면 HRI를 새로 계산하지 않고 상태를 `partial`로 둔다. E를 0으로 저장하지 않는다.

HRI 임계값을 일반 체감온도에 그대로 적용할지 전문가 검토가 필요하다. 대회 시연 전에는 최소한 “의학적 진단 아님”과 데이터 출처를 화면에 표시한다.

### 2.3 API 호출량 계산 오류

`38개 격자 × 하루 48회 = 1,824회`이므로 설계서의 “일 1,000건 내 30분 주기”와 맞지 않는다. 세 종류의 기상 API를 격자별로 호출하면 더 커진다.

**결정:** 전체영역 500m 자료를 한 번 받아 격자에 샘플링하는 방식을 우선 검증한다. 격자별 API만 가능하면 실제 대상자 좌표의 고유 격자 수를 계산하고, 호출 예산을 아래 식으로 배포 전 검사한다.

```text
dailyCalls = uniqueSubjectGrids × runsPerDay × endpointsPerRun
```

호출 예산을 넘으면 60분 주기, 구·군 대표 격자, 운영계정 증액 중 하나를 선택한다. 쉼터 38개 격자가 아니라 **대상자 위치의 고유 격자**를 기준으로 계산한다.

### 2.4 쉼터 상태 제보와 대상자 체크인 혼동

`shelter_reports`에는 `subject_id`가 없다. 따라서 공개 사용자의 “문이 열렸다” 제보를 특정 대상자의 24시간 체크인으로 사용할 수 없다.

**결정:** 두 사건을 분리한다.

- `shelter_reports`: 공개 운영상태·혼잡도 제보, EAS `ShelterStatus` 대상
- `shelter_checkins`: 인증된 담당자 또는 대상자 범위 토큰으로 확인한 실제 방문, EAS `CareEvent(SHELTER_CHECKIN)` 대상

HRI의 C−6은 `shelter_checkins.attestation_state = VERIFIED`인 최신 24시간 기록에만 적용한다. S-06 완료가 자동으로 C−6을 만들지 않는다.

### 2.5 보행 제약을 증명할 데이터 부족

TMAP 보행자 경로 API는 GeoJSON 경로와 거리·시간을 제공하고 `searchOption=30` 요청을 정상 처리했다. 그러나 확인한 응답만으로 경사율과 계단 부재를 증명할 수 없다. 원천 데이터 없이 “경사 5% 초과·계단 절대 배제”를 보장할 수 없다.

**결정:** 지도 표시는 Naver Maps, 보행 후보는 서버에서 호출하는 TMAP `searchOption=30`으로 고정한다. GIS건물통합정보, NGII DEM, OSM 계단, 대구 공원시설물을 보조 자료로 사용하지만 대구 전역의 계단·실제 보행면 경사·300m 휴식 조건은 증명할 수 없다. F-04는 `시연용 그늘·접근성 우선 경로 후보`로 축소하고 `docs/route-data-contract.md`의 불확실성 문구를 항상 표시한다.

### 2.6 휴식 지점 데이터 누락

`최대 무휴식 보행 300m`와 `restSpotDensity`를 계산할 벤치·휴식 지점 데이터가 없다.

**결정:** 대구 공원시설물의 벤치·정자와 일부 구의 그늘막을 `rest_spots`로 적재한다. coverage 밖의 시설 부재는 증명할 수 없으므로 확인된 휴식 지점 사이 간격이 300m를 넘는 후보에는 경고를 붙이되, 도시 전역의 `300m마다 휴식 가능`을 보장하거나 안전 경로로 표시하지 않는다. 밀도 점수는 아래처럼 0~1로 고정한다.

```text
requiredStops = max(1, ceil(distanceM / 300) - 1)
restSpotDensity = min(1, matchedRestSpots / requiredStops)
```

### 2.7 Gemini 모델 ID 불일치

설계서의 `gemini-3-flash`는 현재 안정 API 모델 코드가 아니다. 사용자가 지정하고 실호출로 확인한 안정 모델 `gemini-3.5-flash`가 이미지 입력과 Structured Output을 지원한다.

**결정:** 모델을 코드에 하드코딩하지 않는다.

```env
GEMINI_MODEL=gemini-3.5-flash
```

서버 시작 시 허용 목록과 기능을 확인하고, 모델 교체가 테스트 없이 배포되지 않도록 계약 테스트를 둔다.

### 2.8 알림 채널 미정

보호자 문자/알림톡 발송 공급자, 발신 프로필, 비용, 템플릿 승인이 정해지지 않았다.

**결정:** `NotificationProvider` 인터페이스와 DB outbox를 먼저 구현한다. 개발 환경은 `DemoNotificationProvider`로 딥링크를 화면과 로그의 비민감 필드에만 표시한다. 실제 공급자 선택 전에는 “실제 발송 완료” DoD를 충족하지 못한 것으로 표시한다.

### 2.9 반복 알림 규칙 누락

30분마다 L3/L4인 대상자에게 계속 알림을 보내면 중복 발송과 중복 EAS 기록이 발생한다.

**결정:** 다음 기본 규칙을 사용하고 `상세서.md`에 반영한다.

- L0~L2에서 L3/L4로 진입할 때 1회 발송한다.
- L3에서 L4로 상승할 때 즉시 추가 발송한다.
- 같은 등급이 지속되면 2시간 후 1회만 재알림한다.
- 위험 진입 시 생성한 `episodeId`를 포함해 idempotency key를 `subjectId:episodeId:riskLevel:triggerKind`로 만든다. 회복 후 재진입은 새 episode를 사용하고, 동일 episode의 재실행은 같은 키를 재사용한다.

### 2.10 테스트넷 표시와 해시 규칙

Base Sepolia는 테스트넷이다. 또한 단순 SHA-256은 동일 대상자를 여러 기록에서 쉽게 연결한다.

**결정:** 모든 온체인 화면에 `Base Sepolia 테스트넷`을 표시한다. `subjectHash`는 원본 식별자에 서버 비밀을 적용한 HMAC-SHA-256으로 만들고, `payloadHash`는 정규화된 JSON에 SHA-256을 적용한다. JSON 정규화 방식은 RFC 8785 호환 라이브러리로 고정한다.

## 3. 목표 아키텍처

### 3.1 의존 방향

```text
routes/components
      ↓
application use-cases
      ↓
domain (HRI, 약물 분류, 경로 점수, 해시 규칙)
      ↑
infrastructure adapters (Supabase, KMA, MFDS, Gemini, Naver, TMAP, EAS)
```

- 도메인 함수는 React, Supabase, `fetch`, 환경 변수에 의존하지 않는다.
- 외부 응답은 인프라 어댑터에서 Zod로 파싱한 뒤 도메인 타입으로 변환한다.
- 라우트는 DB 쿼리나 API 키를 직접 다루지 않고 application use-case를 호출한다.
- Supabase secret key, Gemini 키, Naver Client Secret, TMAP appKey, EAS 개인키는 서버 전용 모듈에서만 읽는다.

### 3.2 권장 디렉터리

```text
src/
  components/
    onjung/
      AsyncState.tsx
      Badges.tsx
      Btn.tsx
      Cards.tsx
      FormField.tsx
      Map.tsx
      Ribbon.tsx
      Shells.tsx
      Toast.tsx
  lib/
    auth/
      permissions.ts
      token-session.server.ts
    risk/
      hri.ts
      hri.test.ts
      reasons.ts
    medication/
      schemas.ts
      classify.ts
      classify.test.ts
      scan.server.ts
    routing/
      route-score.ts
      shade.ts
      shade.test.ts
    attestation/
      schemas.ts
      hashing.ts
      eas.server.ts
      verify.server.ts
    integrations/
      kma.server.ts
      mfds.server.ts
      gemini.server.ts
      kakao.server.ts
    supabase/
      browser.ts
      server.ts
      admin.server.ts
      database.types.ts
    env.server.ts
  server/
    jobs/
      recompute-risk.ts
      dispatch-alerts.ts
      process-attestations.ts
  routes/
    __root.tsx
    index.tsx
    login.tsx
    dashboard.tsx
    subjects/$id.tsx
    medication/$subjectId.tsx
    shelters.tsx
    report/$shelterId.tsx
    verify/$uid.tsx
    alert/$eventId.tsx
    api/cron/risk.ts
    api/cron/attestations.ts
scripts/
  prepare-shelters.ts
data/
  daegu_shelters.geojson
supabase/
  migrations/
  seed.sql
  tests/
    rls.test.sql
tests/
  e2e/
```

`routeTree.gen.ts`는 생성 파일이므로 직접 수정하지 않는다.

### 3.3 라우트와 접근 정책

| URL                      | 화면                  | 접근 정책                                      |
| ------------------------ | --------------------- | ---------------------------------------------- |
| `/`                      | 서비스 진입/시연 메뉴 | 공개, 실제 데이터의 민감정보 미노출            |
| `/login`                 | 담당자 로그인         | 공개                                           |
| `/dashboard`             | S-01                  | 인증 + 담당 조직 권한                          |
| `/subjects/$id`          | S-02                  | 인증 + 담당자 배정 또는 관리자                 |
| `/medication/$subjectId` | S-03                  | 인증 담당자 또는 유효한 보호자 세션            |
| `/shelters`              | S-05                  | 공개, 대상자 좌표는 토큰 세션이 있을 때만 사용 |
| `/report/$shelterId`     | S-06                  | 공개 서버 액션 + 속도 제한                     |
| `/verify/$uid`           | S-07                  | 공개, 허용 스키마·발급자 검증                  |
| `/alert/$eventId`        | S-08                  | 1회 토큰 교환 후 24시간 HttpOnly 세션          |

### 3.4 환경 변수

`.env`는 커밋하지 않고 `.env.example`에는 값 없이 키만 기록한다.

```env
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_NAVER_MAPS_NCP_KEY_ID=
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
DATA_GO_SERVICE_KEY=
KMA_APIHUB_AUTH_KEY=
NAVER_MAPS_CLIENT_ID=
NAVER_MAPS_CLIENT_SECRET=
TMAP_APP_KEY=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash
BASE_SEPOLIA_RPC_URL=
EAS_ATTESTER_PRIVATE_KEY=
EAS_CARE_SCHEMA_UID=
EAS_SHELTER_SCHEMA_UID=
EAS_EXPECTED_ISSUER=
SUBJECT_HASH_SECRET=
CRON_SECRET=
```

- `VITE_` 접두사는 브라우저 공개가 허용된 Supabase publishable 값과 Naver `ncpKeyId`에만 쓴다.
- 서버 시작 시 Zod로 누락·형식을 검사한다.
- EAS 개인키와 Supabase secret key를 로그, 오류 응답, 클라이언트 번들에 포함하지 않는다.

## 4. 데이터 모델 보완안

설계서의 5개 테이블만으로 Auth, 체크인, 알림, 캐시, 온체인 재시도를 구현할 수 없다. 다음 테이블을 마이그레이션으로 관리한다.

### 4.1 식별·권한

| 테이블                | 주요 열                                                       | 용도                        |
| --------------------- | ------------------------------------------------------------- | --------------------------- |
| `organizations`       | `id`, `name`, `created_at`                                    | 지자체/수행 조직 경계       |
| `profiles`            | `id -> auth.users`, `organization_id`, `role`, `display_name` | `ADMIN`, `CARE_WORKER` 권한 |
| `subject_assignments` | `subject_id`, `profile_id`                                    | 담당자별 대상자 접근 제한   |

`subjects`에는 `organization_id`, UI가 사용하는 `sex`, 개인정보 변경 시각을 추가한다. 역할 판단에는 사용자 수정 가능한 `user_metadata`를 사용하지 않는다.

### 4.2 위험·기상

| 테이블              | 주요 열                                                             | 용도                       |
| ------------------- | ------------------------------------------------------------------- | -------------------------- |
| `weather_snapshots` | 격자, 체감온도, 특보, 열대야, `source`, `observed_at`, `expires_at` | 외부 기상 캐시와 출처 추적 |
| `risk_snapshots`    | 설계서 열 + `weather_snapshot_id`, `input_hash`                     | 30분 계산 결과와 재현성    |
| `alert_transitions` | 이전/새 등급, idempotency key, 발생 시각                            | 중복 알림 방지             |

`risk_snapshots(subject_id, computed_at desc)`와 `weather_snapshots(kma_nx, kma_ny, observed_at desc)` 인덱스를 둔다. 동일 입력 재계산을 막기 위해 `input_hash`를 인덱싱한다.

### 4.3 복약

| 테이블                     | 주요 열                                                          | 용도                 |
| -------------------------- | ---------------------------------------------------------------- | -------------------- |
| `medication_scan_sessions` | `subject_id`, 이미지 경로, 품질, 상태, 시도 횟수, 모델 ID        | 3단계 판독 흐름 추적 |
| `medications`              | 설계서 열 + `ingredient_name`, `scan_session_id`, `confirmed_by` | 확정 복약 정보       |
| `medication_api_cache`     | API 종류, 요청 해시, 응답, 만료 시각                             | 식약처 30일 캐시     |

이미지는 private Storage bucket `medication-images`에 저장한다. 경로는 `organizationId/subjectId/scanId`로 제한하고, 화면에는 짧은 만료시간의 signed URL만 전달한다. 시연 정책으로 원본 이미지는 저장 완료 후 24시간 안에 삭제한다.

### 4.4 쉼터·경로

| 테이블             | 주요 열                                               | 용도                  |
| ------------------ | ----------------------------------------------------- | --------------------- |
| `shelters`         | 설계서 열                                             | 950개 마스터          |
| `shelter_reports`  | 설계서 열 + `attestation_state`, `client_request_id`  | 운영상태 제보         |
| `shelter_checkins` | `subject_id`, `shelter_id`, 발생 시각, EAS UID·상태   | HRI C−6의 유일한 근거 |
| `buildings`        | PostGIS polygon, 높이, 높이 출처                      | 그림자 생성           |
| `rest_spots`       | PostGIS point, 유형, 출처                             | 300m 휴식 제약        |
| `barrier_segments` | line/polygon, 경사, 계단 여부, 출처                   | 경사·계단 배제        |
| `route_cache`      | 출발 격자, 쉼터, 시간 버킷, geometry, 점수 상세, 만료 | 경로 계산 캐시        |

공간 열에는 GIST 인덱스를 둔다. 공개 쉼터 조회는 `ST_DWithin`로 반경을 제한하고 필요한 열만 반환한다.

### 4.5 돌봄·알림·온체인

| 테이블                | 주요 열                                          | 용도                 |
| --------------------- | ------------------------------------------------ | -------------------- |
| `care_events`         | subject, event type, HRI, payload, 발생 시각     | 오프체인 원본 이벤트 |
| `attestation_jobs`    | 대상 테이블/ID, 상태, 시도, 다음 시도, 오류 코드 | EAS durable retry    |
| `guardian_alerts`     | event, 수신자, 공급자, 상태, 발송 시각           | 알림 outbox          |
| `alert_access_tokens` | alert, token hash, 만료, 교환 시각               | S-08 1회 토큰 교환   |

`care_events`의 개인정보 JSON은 온체인에 직접 넣지 않는다. 온체인에는 HMAC 대상자 해시와 정규화 payload hash만 기록한다.

### 4.6 RLS·GRANT 정책

| 데이터                                      | `anon`                               | `authenticated`              | 서버 secret                          |
| ------------------------------------------- | ------------------------------------ | ---------------------------- | ------------------------------------ |
| `shelters`                                  | 공개 열 SELECT만                     | SELECT                       | 전체                                 |
| `shelter_reports`                           | 직접 접근 금지, 서버 액션으로 INSERT | 직접 접근 금지               | INSERT/UPDATE                        |
| `subjects`, `medications`, `risk_snapshots` | 금지                                 | 조직·배정 범위 SELECT/UPDATE | 배치 전체                            |
| `care_events`, `guardian_alerts`            | 금지                                 | 담당 범위 SELECT             | 발송·배치 전체                       |
| `attestation_jobs`                          | 금지                                 | 금지                         | 전체                                 |
| 공개 검증                                   | DB 직접 공개 금지                    | DB 직접 공개 금지            | 서버가 EAS 조회 후 안전한 DTO만 반환 |

모든 public 테이블에서 기존 권한을 먼저 revoke하고 필요한 작업만 grant한 뒤 RLS를 켠다. 각 정책은 허용·거부 케이스를 pgTAP으로 검사한다. view를 만들면 `security_invoker = true`를 지정한다.

## 5. 공통 구현 규칙

### 5.1 비동기 상태

모든 데이터 컴포넌트는 `idle`, `loading`, `refreshing`, `success`, `empty`, `error`, `partial`을 공통 타입으로 사용한다. 설계서 표에는 6종이라고 적혀 있지만 실제 열거는 7종이므로 문서를 7종으로 고친다.

- `refreshing`: 기존 데이터를 유지한다.
- `partial`: 누락 소스와 마지막 정상 시각을 표시한다.
- `error`: 원인과 다음 행동을 함께 표시한다.
- TanStack Query 키는 도메인별 factory로 만들고, 컴포넌트 안에 임의 문자열 배열을 흩뿌리지 않는다.

### 5.2 외부 API 어댑터

모든 어댑터는 아래 계약을 지킨다.

1. `AbortSignal.timeout()` 또는 동등한 방식으로 타임아웃을 둔다.
2. HTTP 성공 여부와 응답 JSON을 Zod로 검증한다.
3. 외부 오류를 `ExternalServiceError`의 `service`, `code`, `retryable`로 변환한다.
4. 로그에는 요청 ID, 서비스, 지연시간, 상태만 남기고 키·전화·이름·이미지를 제외한다.
5. 캐시 키에는 정규화된 요청값과 API 버전을 포함한다.
6. 테스트에서는 실제 외부망 대신 저장된 계약 fixture와 MSW를 사용한다.

### 5.3 보안·개인정보

- 이름·전화·주소·복약·이미지를 클라이언트 로그와 분석 도구에 보내지 않는다.
- 전체 이름 보기 토글은 권한 확인 후 서버가 별도 요청으로 반환하고 감사 로그를 남긴다.
- 공개 링크에는 subject ID, 전화번호, 이름을 넣지 않는다.
- 토큰 원문은 DB에 저장하지 않고 SHA-256 hash만 저장한다.
- 민감한 서버 액션에 CSRF, 입력 검증, 속도 제한, idempotency key를 적용한다.
- 테스트와 시연은 가상 대상자만 사용한다.

### 5.4 접근성·UI

- 루트 문서에 `lang="ko"`와 `viewport-fit=cover`를 적용한다.
- 모든 화면 첫 요소로 본문 건너뛰기 링크를 둔다.
- 지도와 같은 정보를 리스트에서 검색·정렬·선택할 수 있게 한다.
- 위험 등급은 색, 라벨, 형태를 함께 표시한다.
- 모달·바텀시트는 Radix 기반 포커스 트랩을 사용한다.
- L4 애니메이션은 reduced-motion에서 2px 정적 보더로 바꾼다.
- Paper 본문 18px, 시니어 22px와 56×56px 탭 영역을 Playwright viewport 테스트로 확인한다.

### 5.5 완료 검증 명령

Phase 0에서 다음 스크립트를 만든 뒤 모든 phase 종료 때 실행한다.

```bash
bun run lint
bun run typecheck
bun run test
bun run test:e2e
bun run build
```

DB 변경 phase에서는 아래 검증을 추가한다.

```bash
supabase db reset
supabase test db
supabase gen types typescript --local
```

## 6. Phase별 구현 계획

각 phase는 앞 phase의 DoD를 통과한 뒤 시작한다. 한 phase 안에서도 번호 순서대로 작업한다.

### Phase 0. 명세 잠금과 개발 기준선 복구 — P0

**목표:** 구현 중간에 프레임워크, 데이터 소스, 외부 공급자를 다시 고르지 않도록 선행 결정을 문서와 설정에 고정한다.

#### 작업

- [x] **P0-01. 프레임워크 결정을 기록한다.** `상세서.md` §2.1을 TanStack Start/Vite 기준으로 갱신하고, Next.js 전용 `/app` 구조를 `src/routes` 구조로 바꾼다.
- [x] **P0-02. 패키지 관리자를 Bun으로 통일한다.** `package.json`에 `packageManager`를 기록하고 README의 `npm i`를 `bun install --frozen-lockfile`로 바꾼다. `bun.lock`만 잠금파일로 유지한다.
- [x] **P0-03. 로컬 의존성을 복구한다.** 프로젝트 루트에서 `bun install --frozen-lockfile`을 실행한 뒤 현재 소스를 수정하지 않은 상태의 build/lint/typecheck 결과를 기준선 로그로 남긴다.
- [x] **P0-04. 검증 스크립트를 추가한다.** `typecheck`, `test`, `test:watch`, `test:e2e` 스크립트와 Vitest, React Testing Library, MSW, Playwright, axe 의존성을 잠금파일에 고정한다.
- [x] **P0-05. 환경 변수 계약을 만든다.** `.env.example`, `src/lib/env.server.ts`를 만들고 `.gitignore`에 `.env`, `.env.*`, `!.env.example`을 명시한다.
- [x] **P0-06. 외부 API 최신 문서를 다시 확인한다.** 구현 시점의 Supabase changelog, Gemini 모델 목록, Naver Maps·TMAP 정책·쿼터, KMA 제공 목록을 확인하고 확인 날짜와 URL을 `docs/external-contracts.md`에 기록한다.
- [x] **P0-07. 기상 소스 spike를 수행한다.** 대구 좌표 1곳으로 500m `ta_chi`, TMP/REH, 특보 응답을 호출하고 Zod 초안과 fixture를 만든다. 사용할 1차·2차 소스를 `상세서.md`에 확정한다. (`src/integrations/kma/weather.ts`, 비밀키 없는 fixture·테스트)
- [x] **P0-08. 호출량 예산을 계산한다.** 실제 가상 대상자 5명의 고유 격자 수와 30분 주기 일 호출량을 표로 만들고 계정 한도 이하임을 증명한다. (`docs/weather-call-budget.md`)
- [x] **P0-09. 보행 제약 데이터 소스를 확정한다.** 건물 높이, 계단, 경사/표고, 휴식 지점의 공급처·좌표계·라이선스·갱신일을 기록한다. 대구 전역 공식 계단 자료와 보행면 경사·휴식 coverage가 없으므로 F-04를 “시연용 그늘·접근성 우선 경로 후보”로 축소했다. (`docs/route-data-contract.md`)
- [x] **P0-10. 알림 공급자를 확정한다.** 현재 릴리스는 외부 발송이 없는 `DemoNotificationProvider`로 고정하고, 운영 전환에 필요한 발신 프로필·템플릿 승인·키·쿼터·수신동의 게이트를 문서화했다. (`docs/notification-provider.md`)
- [x] **P0-11. 배포 대상을 고정한다.** Lovable 래퍼의 Nitro `vercel` preset을 고정하고 Build Output API v3·SSR catch-all 검증기와 배포 문서를 추가했다. (`vite.config.ts`, `scripts/verify-vercel-build.mjs`, `docs/deployment-vercel.md`)
- [x] **P0-12. 설계 충돌을 반영한다.** 제보/체크인 분리, 7종 비동기 상태, 반복 알림 규칙, Base Sepolia 테스트넷 표시, HMAC subject hash를 `상세서.md` v1.2에 기록했다.
- [ ] **P0-13. 버전 관리 상태를 복구한다.** 현재 폴더에는 `.git`이 없으므로 Lovable에 연결된 원격 저장소의 정상 clone인지 확인한다. 새 저장소를 임의로 만들기보다 원격 이력을 보존한 clone에서 구현을 시작한다.

#### 산출물

- 갱신된 `상세서.md`
- `.env.example`
- `docs/external-contracts.md`
- 테스트·타입·E2E 스크립트가 있는 `package.json`
- 잠금파일 기준 로컬 설치와 기준선 검사 결과

#### DoD

- `bun run lint`, `bun run typecheck`, `bun run build`가 현재 프로토타입에서 통과하거나, 남은 각 오류가 파일·행·해결 phase와 함께 기록돼 있다.
- 기상, 보행 제약, 알림 공급자에 “추후 결정”만 남아 있지 않다.
- 브라우저 번들에 들어갈 키와 서버 전용 키가 구분돼 있다.

---

### Phase 1. 도메인 기준·테스트·디자인 시스템 완성 — P0

**목표:** 이후 모든 기능이 공유할 계산식, 타입, 상태 UI, 접근성 기준을 고정한다.

#### 1. HRI 엔진

- [x] **P1-01. HRI 계산을 순수 모듈로 유지한다.** `src/lib/risk/hri.ts`에는 입력→결과 계산만 두고 UI 문구 생성은 `src/lib/risk/reasons.ts`로 분리한다.
- [x] **P1-02. 이유 점수 표시를 고친다.** 체감온도 기본점수, 특보 가산, 열대야 가산을 각각 계산하고 E 상한으로 실제 반영된 기여도를 배분한다. `체감 39.2℃ + 폭염경보 (+47)`처럼 표시하고 열대야를 중복 합산하지 않는다.
- [x] **P1-03. 입력 불변식을 파싱한다.** 비정상 수치, 음수 약물 계열 수, 0 미만 나이, `NaN`을 거부하는 `HriInputSchema`를 만든다.
- [x] **P1-04. HRI 단위 테스트를 만든다.** 다음을 표 기반 테스트로 검증한다.
  - 체감온도 `30.9`, `31`, `33`, `35`, `38`, `40`
  - WATCH `+3`, WARNING `+5`, 열대야 3일 `+5`, E 상한 50
  - 같은 약물 계열 중복 제거, M 상한 25, 미등록 M=0 경고
  - 65/75/85세 경계, P 상한 20
  - 검증된 24시간 체크인만 C=6
  - HRI 19/20/39/40/59/60/79/80 등급 경계
  - 이유 최대 3개와 기여도 순서

#### 2. 공통 타입과 상태

- [x] **P1-05. 도메인 열거형을 한곳에 둔다.** `RiskLevel`, `AttestState`, `ShelterOpen`, `CrowdLevel` 등을 `src/lib/domain-types.ts` 또는 도메인별 파일에서 export하고 mock 전용 타입과 분리한다.
- [x] **P1-06. 비동기 상태 컴포넌트를 만든다.** `AsyncState`, `EmptyState`, `ErrorState`, `PartialDataBanner`, `RefreshingIndicator`를 구현한다.
- [x] **P1-07. 오류 DTO를 고정한다.** 사용자 메시지, 재시도 가능 여부, 오류 코드를 분리하고 내부 stack이나 외부 원문을 UI에 노출하지 않는다.

#### 3. 디자인 시스템

- [x] **P1-08. CSS 토큰 누락을 채운다.** 간격, z-index, surface shadow, safe-area 좌우/하단, `scroll-margin-top`, senior 버튼·아이콘 토큰을 `src/styles.css`에 추가한다.
- [x] **P1-09. 폰트를 적용한다.** Wanted Sans, Pretendard, JetBrains Mono의 허용 라이선스와 파일을 확인하고 preload + `font-display: swap`을 설정한다. 파일을 포함하지 않으면 시스템 fallback 사용 사실을 명시한다.
- [x] **P1-10. Button을 링크와 안전하게 공유한다.** `Btn`에 `asChild` 또는 `ButtonLink`를 추가해 `<a><button /></a>` 중첩을 제거하고 `senior` size를 구현한다.
- [x] **P1-11. 공통 컴포넌트를 완성한다.** `RiskCard`, `ShelterCard`, `FormField`, `Modal/BottomSheet`, `Toast`, `DataTable`, `MapFallbackList`를 명세 규격으로 만든다.
- [x] **P1-12. 루트 문서를 한국어로 고친다.** `src/routes/__root.tsx`, `src/lib/error-page.ts`에서 `lang="ko"`, `viewport-fit=cover`, 온중 메타데이터, 한국어 404/500, 건너뛰기 링크를 적용한다.
- [x] **P1-13. 시니어 설정 우선순위를 구현한다.** 서버 프로필 → localStorage → 기본값 순으로 적용하고 hydration 깜빡임을 막는다.

#### 4. 컴포넌트 검증

- [x] **P1-14. 컴포넌트 테스트를 작성한다.** RiskBadge 3중 인코딩, Btn loading/disabled, Modal focus 복귀, Toast aria-live, senior 탭 크기를 검사한다.
- [x] **P1-15. Story/demo route를 만든다.** 운영 라우트와 분리된 개발 전용 화면에서 모든 상태·surface·viewport를 확인한다. production build에서는 노출하지 않는다.

#### DoD

- HRI 단위 테스트가 모든 경계값에서 통과한다.
- 공통 컴포넌트에 중첩 interactive element가 없다.
- 키보드만으로 모달 열기·닫기·포커스 복귀가 가능하다.
- 360px Paper와 1024px Shade의 기본 레이아웃이 가로 스크롤 없이 렌더된다.

---

### Phase 2. Supabase·Auth·쉼터 시드 기반 구축 — P0

**목표:** mock을 교체할 안전한 영속 계층과 접근 제어를 만든다.

#### 1. Supabase 프로젝트와 마이그레이션

- [x] **P2-01. CLI 명령을 확인한다.** 설치된 Supabase CLI에서 `supabase --help`, `supabase migration --help`, `supabase gen types --help`를 실행하고 현재 문법을 확인한다.
- [x] **P2-02. 로컬 프로젝트를 초기화한다.** `supabase/`를 만들고 PostGIS, pgcrypto, 테스트에 필요한 extension을 마이그레이션으로 활성화한다. 각 파일은 `supabase migration new <name>`으로 만들고 임의 timestamp 파일명을 작성하지 않는다. extension 버전은 명시하지 않는다.
- [x] **P2-03. 식별·권한 테이블을 만든다.** `organizations`, `profiles`, `subject_assignments`를 추가한다.
- [x] **P2-04. 핵심 도메인 테이블을 만든다.** 보완된 `subjects`, `shelters`, `medication_scan_sessions`, `medications`, `weather_snapshots`, `risk_snapshots`, `shelter_reports`, `shelter_checkins`, `care_events`, `guardian_alerts`, `alert_access_tokens`, `attestation_jobs`를 추가한다.
- [x] **P2-05. 공간·성능 인덱스를 만든다.** Phase 2 테이블의 subject/shelter GIST와 subject/time, risk/time, job/state/next_attempt 인덱스를 만든다. 건물·휴식·장애물 GIST는 출처·CRS 계약과 함께 P6-01~04에서 추가한다.
- [x] **P2-06. enum/check constraint를 적용한다.** HRI 0~~100, crowd 0~~2, 위험 등급, source, confidence 0~1, 미래가 아닌 observed_at을 DB에서도 제한한다.
- [x] **P2-07. 최소 권한을 적용한다.** 각 테이블의 `anon`, `authenticated` 자동 권한을 revoke하고 필요한 동작만 grant한 뒤 RLS 정책을 작성한다.
- [x] **P2-08. RLS 테스트를 작성한다.** 다른 조직 대상자 조회 거부, 담당자 배정 범위, public shelter 열 제한, anon 민감 테이블 거부, 서버 작업 허용을 pgTAP으로 검증한다.

#### 2. Supabase 클라이언트와 타입

- [x] **P2-09. 클라이언트를 역할별로 분리한다.** browser publishable client, 사용자 세션 server client, 배치용 admin server client를 서로 다른 파일에 둔다.
- [ ] **P2-10. 타입을 생성한다.** 마이그레이션 후 `src/lib/supabase/database.types.ts`를 생성하고 CI에서 schema diff와 타입 재생성 누락을 검사한다.
- [ ] **P2-11. 인증 가드를 만든다.** `/dashboard`, `/subjects/*`, 담당자 방식의 `/medication/*`에 beforeLoad/server guard를 적용한다.
- [x] **P2-12. 마스킹 권한을 만든다.** 기본 DTO는 masked name과 축약 주소만 반환하고 전체 개인정보는 권한 확인 서버 함수로만 반환한다.

#### 3. 쉼터 ETL

- [x] **P2-13. 전처리 스크립트를 만든다.** `scripts/prepare-shelters.ts`가 EUC-KR DBF를 읽고 950건의 WGS84 GeoJSON을 만든다.
- [x] **P2-14. 파생값을 계산한다.** 앞서 검증한 분류 정규식, 구·군 파싱, `is_im_bank`, 안정 ID, KMA 격자 좌표를 기록한다.
- [x] **P2-15. ETL 불변식을 검사한다.** 총 950, 유형 466/245/129/110, iM 100, 8개 구·군, 고유 ID 950, 대구 경계 내 좌표를 만족하지 않으면 스크립트를 실패시킨다.
- [x] **P2-16. 시드를 만든다.** `data/daegu_shelters.geojson`과 재실행 가능한 `supabase/seed.sql` 또는 import 스크립트를 만든다.
- [x] **P2-17. 가상 대상자 5명을 시드한다.** 동의 시각, 담당자 배정, 복약 등록/미등록, 체크인 유무, L0~L4 시나리오를 포함한다. 실제 개인정보를 사용하지 않는다.

#### DoD

- `supabase db reset` 한 번으로 스키마, RLS, 쉼터 950건, 가상 대상자 5명이 재현된다.
- 쉼터 집계가 950/466/245/129/110/100과 정확히 일치한다.
- anon이 이름·전화·주소·복약·risk snapshot을 읽지 못한다.
- 한 조직의 담당자가 다른 조직 대상자를 읽거나 수정하지 못한다.

> 현재 환경에는 Docker/PostgreSQL이 없어 `supabase db reset`, 실제 pgTAP, 로컬 DB 기반 타입 생성을 실행할 수 없다. 마이그레이션·시드·RLS 정적 계약과 CI 게이트는 작성했으며, P2-10과 이 DoD의 런타임 확인은 Docker 사용 가능 환경에서 완료한다.

---

### Phase 3. 실기상 배치·S-01·S-02 완성 — P0

**목표:** F-01을 실제 데이터로 동작시키고 대시보드를 운영 데이터 검증 창구로 만든다.

#### 1. 기상 어댑터와 캐시

- [ ] **P3-01. KMA 스키마를 고정한다.** Phase 0 fixture에서 필요한 필드만 Zod로 파싱하고 원본 필드명 변경을 계약 테스트로 감지한다.
- [ ] **P3-02. 1차·2차 소스를 구현한다.** 확정한 `ta_chi` 또는 TMP/REH Stull 계산을 `src/lib/integrations/kma.server.ts`에 구현한다.
- [ ] **P3-03. 특보를 별도 조회한다.** 폭염주의보·경보를 지역과 발효시각으로 정규화하고 기온 임계값으로 특보를 추정하지 않는다.
- [ ] **P3-04. 열대야 streak를 계산한다.** KST 날짜별 야간 최저기온 25℃ 이상 여부를 저장하고 연속 일수를 계산한다. 데이터 부족 시 0으로 단정하지 않고 `partial`을 기록한다.
- [ ] **P3-05. 캐시 정책을 구현한다.** 정상값 25분 TTL, 최근 유효값 최대 3시간, 출처·수집시각·오류 상태를 `weather_snapshots`에 저장한다.
- [ ] **P3-06. fail-safe 테스트를 작성한다.** 1차 실패→2차, 2차 실패→캐시, 전부 실패→마지막 E 유지 흐름을 fixture로 검증한다.

#### 2. 위험도 배치

- [ ] **P3-07. `recompute-risk` use-case를 만든다.** 대상자, 최신 복약 계열, 최신 검증 체크인, 기상을 한 번에 불러와 `computeHri()`를 호출한다.
- [ ] **P3-08. 스냅샷을 멱등 저장한다.** 입력 hash가 같은 subject/time bucket의 중복 insert를 막는다.
- [ ] **P3-09. 등급 전이를 만든다.** 이전 스냅샷과 비교해 L3/L4 진입·상승·2시간 지속 이벤트만 `alert_transitions`와 `care_events`에 기록한다.
- [ ] **P3-10. Cron endpoint를 보호한다.** `/api/cron/risk`에서 `CRON_SECRET`을 검증하고 동시 실행 lock, 실행 요약, 실패한 subject 수를 기록한다.
- [ ] **P3-11. 배치 통합 테스트를 작성한다.** 5명 시드에 대해 예상 HRI·등급·전이·중복 방지를 검증한다.

#### 3. S-01 대시보드

- [ ] **P3-12. mock import를 제거한다.** loader/server function과 TanStack Query로 실제 요약, 긴급 대상자, 지도 DTO, care event를 조회한다.
- [ ] **P3-13. 검색 상태를 URL에 넣는다.** `gu`, `level`, `sort`, `order`를 Zod search schema로 파싱하고 브라우저 뒤로가기를 보존한다.
- [ ] **P3-14. 30초 갱신을 실제 query invalidation으로 바꾼다.** `refreshing` 동안 현재 데이터를 유지하고 마지막 갱신시각을 표시한다.
- [ ] **P3-15. 신규 L4 경보를 구현한다.** 아직 확인하지 않은 transition만 assertive alert로 표시하고 사용자별 확인 상태를 저장한다.
- [ ] **P3-16. Naver 지도를 아직 연결하지 않은 경우 접근 가능한 리스트를 우선 제공한다.** Phase 5 전까지 지도 패널은 명확한 준비 상태로 표시하고 가짜 마커를 제거한다.
- [ ] **P3-17. 모든 데이터 상태를 구현한다.** loading, refreshing, empty, error, partial을 Story와 테스트에서 재현한다.

#### 4. S-02 대상자 상세

- [ ] **P3-18. `/subjects/$id`를 만든다.** 대상자 DTO, 최신 HRI breakdown, 복약 목록, care events를 권한 범위 안에서 조회한다.
- [ ] **P3-19. 위험도 구성 막대를 구현한다.** E/50, M/25, P/20, C/6 비율과 실제 합계를 텍스트로 함께 제공한다.
- [ ] **P3-20. 개인정보 토글을 보호한다.** 기본은 마스킹하고 권한 있는 사용자가 명시적으로 열 때만 원문을 요청한다.
- [ ] **P3-21. 복약 미등록·이력 빈 상태를 구현한다.** 명세 문구와 구체적 CTA를 사용한다.
- [ ] **P3-22. 액션을 실제 상태와 연결한다.** 약봉투 촬영은 Phase 4, 쉼터 경로는 Phase 6, 보호자 알림은 Phase 7 기능 플래그 뒤에서 연결한다.

#### DoD

- 30분 배치 2회 실행 시 동일 입력 중복 스냅샷과 중복 알림 이벤트가 생기지 않는다.
- 기상 API가 실패해도 E가 0으로 떨어지지 않고 이전 값과 `partial` 배너가 보인다.
- S-01 URL을 공유하면 동일 필터·정렬이 복원된다.
- S-02에서 권한 없는 사용자는 전체 이름·전화·주소를 받을 수 없다.

---

### Phase 4. F-02 약봉투 판독과 S-03 — P1

**목표:** 촬영부터 13계열 매칭, 사용자 확인, 저장, HRI 즉시 반영까지 완성한다.

#### 1. 이미지 입력과 스캔 세션

- [ ] **P4-01. `/medication/$subjectId`를 만든다.** 촬영, 결과 확인, 저장 완료를 URL 또는 명시적 step state로 관리한다.
- [ ] **P4-02. 파일을 클라이언트에서 검증한다.** `image/*`, 최대 10MB, EXIF 회전, 장변 2048px, 최종 MIME·크기를 확인한다.
- [ ] **P4-03. 권한 거부 폴백을 구현한다.** 카메라 거부 시 앨범 선택과 직접 입력을 즉시 제공하고 24시간 동안 재요청 모달을 띄우지 않는다.
- [ ] **P4-04. private Storage에 업로드한다.** 서버가 subject 권한과 경로를 검증한 뒤 scan session을 만든다.

#### 2. Gemini 판독

- [ ] **P4-05. `MedicationExtractionSchema`를 Zod로 만든다.** Gemini Structured Output과 서버의 최종 parse에 같은 스키마를 사용한다.
- [ ] **P4-06. 서버 전용 Gemini 어댑터를 만든다.** `@google/genai`, `GEMINI_MODEL`, 15초 타임아웃, 1회 재시도, 모델 ID 기록을 적용한다.
- [ ] **P4-07. 이미지 품질 상태를 구현한다.** BLURRY/PARTIAL/UNREADABLE 문구, 재촬영, 3회 실패 후 수동 전환을 서버 session의 attempt count와 일치시킨다.
- [ ] **P4-08. 스키마 실패 폴백을 구현한다.** 재프롬프트 1회 후 raw text만 안전하게 표시하고 사용자가 확정하기 전 저장하지 않는다.

#### 3. 식약처 조회와 분류

- [ ] **P4-09. 식약처 3개 어댑터를 분리한다.** 낱알식별, e약은요, DUR 응답을 각각 Zod로 파싱하고 30일 캐시한다.
- [ ] **P4-10. 후보 매칭 순서를 고정한다.** 품목명 exact/normalized → 품목코드 → 각인+모양+색 순으로 조회하고 모호한 후보는 사용자에게 선택시킨다.
- [ ] **P4-11. 13계열 분류를 데이터로 관리한다.** 성분명 정규화, 동의어, HIGH/MID/NONE을 `classify.ts`에 두고 제품명 문자열만으로 확정하지 않는다.
- [ ] **P4-12. 의료 매핑 fixture를 검수한다.** 부록 B의 대표 성분, 비해당 약, 동명이품, 1세대/2세대 항히스타민 구분을 테스트한다.
- [ ] **P4-13. confidence 규칙을 구현한다.** ≥0.85 자동, 0.60~0.84 확인, <0.60 제외+직접입력을 정확히 적용한다.

#### 4. 확인·저장·HRI 반영

- [ ] **P4-14. 결과 편집 폼을 만든다.** React Hook Form + Zod를 사용하고 첫 오류 필드로 포커스를 이동한다.
- [ ] **P4-15. 선택 항목을 트랜잭션으로 저장한다.** 기존 약물 교체/추가 정책을 명시하고 `source`, `confidence`, `confirmed_by`를 남긴다.
- [ ] **P4-16. HRI를 즉시 재계산한다.** 저장 전후 스냅샷을 만든 뒤 `60 → 72 (+12)`를 실제 결과로 표시한다.
- [ ] **P4-17. 등급 전이를 연결한다.** 복약 저장으로 L3/L4에 진입하면 Phase 3의 transition/outbox를 한 번만 생성한다.
- [ ] **P4-18. 면책 문구를 고정한다.** 결과, 확인, 저장 완료 화면 하단에 명세 문구를 항상 표시한다.

#### 테스트와 DoD

- Gemini 정상/timeout/quota/schema 오류, 식약처 0건/1건/복수건, 이미지 품질 3회 실패를 계약 테스트한다.
- 수동 입력만으로도 복약 저장과 HRI 재계산을 끝낼 수 있다.
- HIGH/MID 중복 계열은 한 번만 점수에 반영된다.
- 저장 실패 시 입력과 판독 결과를 유지하고 재시도할 수 있다.
- 원본 이미지·API 키·복약명이 클라이언트 오류 로그에 남지 않는다.

---

### Phase 5. 쉼터 조회·Naver Map·S-06 제보 — P1

**목표:** 시민이 실제 950개 쉼터를 위치·상태·iM뱅크 여부로 찾고 운영상태를 제보할 수 있게 한다. 그늘 경로 계산은 Phase 6에서 추가한다.

#### 1. 쉼터 조회 API

- [ ] **P5-01. 반경 조회 use-case를 만든다.** WGS84 위치, 반경 500m/1km/3km, 구·군, iM뱅크, 운영상태를 Zod로 파싱하고 PostGIS `ST_DWithin`로 조회한다.
- [ ] **P5-02. 공개 DTO를 제한한다.** 쉼터 ID, 명칭, 유형, 구·군, 도로명주소, 좌표, iM 여부, 최신 상태·시각·검증 상태만 반환한다.
- [ ] **P5-03. 최신 상태를 계산한다.** 쉼터별 최신 제보 1건을 사용하고 2시간이 지나면 `UNKNOWN`으로 처리한다. 2시간 기준은 `상세서.md`에 추가한다.
- [ ] **P5-04. 기본 정렬을 구현한다.** Phase 5에서는 `검증된 OPEN + 500m 이내`를 우선하고 거리 오름차순으로 정렬한다. Phase 6에서 shadeRatio를 두 번째 키로 추가한다.
- [ ] **P5-05. 빈 상태를 구현한다.** 500m 0건이면 1km 확대, 필터 0건이면 초기화 CTA를 제공한다.

#### 2. S-05 지도와 목록

- [ ] **P5-06. `/shelters`를 만든다.** Paper 모바일 1열, 768px 중앙, 1024px 이상 지도 7:list 5 레이아웃을 구현한다.
- [ ] **P5-07. Naver Map JS loader를 만든다.** `ncpKeyId`, 허용 도메인, 로딩/실패 상태와 `window.navermap_authFailure`를 관리하고 중복 script 삽입을 막는다.
- [ ] **P5-08. 마커를 구현한다.** 일반 OPEN/UNKNOWN, iM뱅크, 현재 위치를 서로 다른 색·형태·크기로 표시하고 50개 초과 시 클러스터링한다.
- [ ] **P5-09. 목록을 실제 콘텐츠로 둔다.** 지도 컨테이너는 `aria-hidden="true"`로 처리하고 필터·정렬·쉼터 선택·경로 요청을 목록만으로 완료할 수 있게 한다.
- [ ] **P5-10. 위치 권한을 단계적으로 요청한다.** 페이지 진입 즉시가 아니라 `내 위치로 찾기` 액션 후 요청한다. 거부하면 주소 검색으로 전환하고 24시간 동안 재요청하지 않는다.
- [ ] **P5-11. Naver 주소 검색을 서버에서 호출한다.** Client Secret을 브라우저에 노출하지 않고 주소→좌표 후보를 반환한다. Reverse Geocoding은 권한 승인 후 별도 연결한다.
- [ ] **P5-12. 필터를 URL에 보존한다.** `lat`, `lng`, `radius`, `imBank`, `open`, `sort`를 search schema로 검증한다. 민감한 subject ID나 전체 주소는 URL에 넣지 않는다.
- [ ] **P5-13. 쉼터 카드를 완성한다.** 이름 2줄, 주소 1줄, 거리, 노인 보행시간, 최신 제보, AttestBadge, iM 칩을 표시한다. Phase 6 전에는 shade 값을 숨긴다.

#### 3. S-06 운영상태 제보

- [ ] **P5-14. `/report/$shelterId`를 만든다.** 열림/닫힘 radio group과 선택형 혼잡도를 48px 이상 탭 영역으로 구현한다.
- [ ] **P5-15. 익명 제보 세션을 만든다.** 서버가 발급한 임의 식별자를 HttpOnly/SameSite cookie로 저장하고 HMAC reporter hash를 계산한다.
- [ ] **P5-16. 10분 중복 제보를 DB에서 막는다.** 동일 reporter hash + shelter의 최근 10분 기록을 트랜잭션 안에서 검사한다. 클라이언트 타이머만 믿지 않는다.
- [ ] **P5-17. 속도 제한을 추가한다.** IP 원문을 저장하지 않고 edge/server rate limit을 적용한다. 제한 오류에는 다시 가능한 시각을 표시한다.
- [ ] **P5-18. 낙관적 상태를 구현한다.** DB insert 성공 후 UI를 즉시 갱신하되, Phase 7 전에는 `UNVERIFIED`, EAS 작업 생성 후에는 `PENDING`으로 표시한다.
- [ ] **P5-19. 체크인과 분리한다.** 공개 운영상태 제보는 `shelter_checkins`나 HRI C에 영향을 주지 않는 테스트를 작성한다.

#### DoD

- 950개 쉼터가 실제 PostGIS 쿼리 결과로 표시된다.
- iM 필터 결과가 100건이며 전용 마커를 사용한다.
- 위치 권한을 거부해도 주소 검색으로 모든 조회와 선택을 끝낼 수 있다.
- 지도 없이 목록만으로 같은 쉼터 정보를 찾고 제보 페이지로 이동할 수 있다.
- 같은 익명 사용자의 10분 내 중복 제보가 서버에서 차단된다.

---

### Phase 6. F-04 시연용 그늘·접근성 우선 경로와 검증된 체크인 — P1

**선행조건:** Phase 0의 `docs/route-data-contract.md`에 확정한 부분 coverage와 보장 경계를 그대로 적용한다.

**목표:** 최대 3개 도보 후보를 노인 보행·그늘·확인된 휴식·장애물 증거 기준으로 평가하고, 불확실성과 함께 S-05에 표시한다.

#### 1. 공간 데이터 ETL

- [ ] **P6-01. 건물 데이터를 적재한다.** footprint, 높이, 높이 출처, 갱신일을 WGS84/PostGIS geometry로 변환한다. 높이 누락 시 층수 기반 추정 여부를 별도 필드에 남긴다.
- [ ] **P6-02. 휴식 지점을 적재한다.** 벤치·정자·그늘막 등 허용 유형과 출처를 `rest_spots`에 넣는다.
- [ ] **P6-03. 확인된 장애물 증거를 적재한다.** OSM 계단 구간과 DEM 파생 5% 초과 경사 위험 구간을 `barrier_segments`에 넣고 출처·기준일·coverage·confidence·unknown reason을 기록한다.
- [ ] **P6-04. ETL 품질 검사를 만든다.** 대구 경계 내 geometry, 유효 polygon, 양수 높이, 중복률, 데이터 갱신일을 검사한다.

#### 2. TMAP 보행 경로 후보

- [ ] **P6-05. TMAP pedestrian 어댑터를 만든다.** 서버에서 `routes/pedestrian`을 호출하고 GeoJSON features를 LineString과 거리·시간으로 정규화한다. 지원이 공식 확인된 option만 후보 생성에 사용한다.
- [ ] **P6-06. 후보를 정규화한다.** 동일 geometry가 95% 이상 겹치면 중복을 제거하고 최대 3개를 유지한다.
- [ ] **P6-07. 노인 보행시간을 재계산한다.** TMAP 기본 시간을 그대로 표시하지 않고 `ceil(distanceM / 0.75)`초를 사용한다.
- [ ] **P6-08. 확인된 위험 증거를 적용한다.** TMAP `searchOption=30` 후보 중 확인된 계단 또는 5% 초과 DEM 경사 위험과 교차하는 경로를 제외한다. 휴식 간격 300m 초과와 coverage 공백은 경고·unknown reason으로 남기며 어떤 후보도 `VERIFIED_SAFE`로 표시하지 않는다.

#### 3. 그림자와 점수

- [ ] **P6-09. 태양 위치를 계산한다.** `suncalc`로 KST 시각의 altitude/azimuth를 구하고 altitude≤0이면 shade 계산을 건너뛴다.
- [ ] **P6-10. 그림자 polygon을 만든다.** 경로 30m 주변 건물만 조회하고 `height / tan(altitude)` 길이를 태양 반대 방향으로 투영한다.
- [ ] **P6-11. 경로를 그늘/햇빛 segment로 나눈다.** Turf 교차·길이 계산으로 `shadeDistanceM`, `sunDistanceM`, `shadeRatio`를 구하고 총 길이와 오차 1% 이내인지 검사한다.
- [ ] **P6-12. 점수 정규화를 고정한다.** 후보 집합에서 아래 식을 사용한다.

```text
durationNorm = maxDuration == minDuration
  ? 0
  : (durationSec - minDuration) / (maxDuration - minDuration)

routeScore = shadeRatio * 0.60
           + (1 - durationNorm) * 0.25
           + restSpotDensity * 0.15
```

- [ ] **P6-13. 일몰 후 규칙을 구현한다.** 확인된 위험 증거와 교차하지 않는 후보 중 최단 경로를 선택하고 `일몰 후 — 최단 경로로 안내합니다` 배너와 접근성 불확실성 안내를 함께 표시한다.
- [ ] **P6-14. 캐시를 구현한다.** 출발 위치를 작은 격자로 양자화하고 destination + 10분 태양 버킷 + 공간 데이터 버전으로 cache key를 만든다.
- [ ] **P6-15. 알고리즘 테스트를 작성한다.** 수평/수직 건물, 낮은 태양, 일몰, 경로 교차, 후보 동률, 휴식 간격, 계단·경사 제외를 고정 fixture로 검사한다.

#### 4. 경로 UI와 체크인

- [ ] **P6-16. S-05 정렬을 완성한다.** 검증 OPEN+500m → shadeRatio 내림차순 → 거리 오름차순을 적용한다.
- [ ] **P6-17. 경로 polyline을 그린다.** 그늘 청록 6px, 햇빛 주황 6px, 대체 경로 점선 4px를 표시한다.
- [ ] **P6-18. 경로 상세 BottomSheet를 만든다.** 거리, 0.75m/s 시간, 그늘 비율, 햇빛 구간 수·합계, 휴식 지점을 텍스트와 구간 바로 제공한다.
- [ ] **P6-19. Naver 지도 앱/웹 landing URL을 연결한다.** 새 앱/탭 이동을 사전에 알리고 돌아왔을 때 상태를 보존한다.
- [ ] **P6-20. 체크인 액션을 제한한다.** 인증 담당자 또는 S-08 subject-scoped 세션에서만 `shelter_checkins`를 생성한다. 일반 공개 사용자는 운영상태 제보만 가능하다.
- [ ] **P6-21. C−6 대기 상태를 표현한다.** 체크인 직후 `기록 확인 중`을 표시하고 EAS가 VERIFIED가 된 후 다음 HRI 재계산에서만 C=6을 적용한다.

#### DoD

- 서로 다른 route mode에서 최대 3개 후보를 만들고 같은 입력에 결정적인 순서를 반환한다.
- 샘플 경로의 그늘 거리 + 햇빛 거리가 전체 거리와 1% 이내로 맞는다.
- 계단·경사·300m 휴식 조건을 증명할 수 없는 경로를 “안전 경로”로 표시하지 않는다.
- 일몰 후에는 shadeRatio 대신 최단 경로와 명세 배너가 표시된다.
- 공개 제보가 아닌 검증된 subject check-in만 HRI C−6을 만든다.

---

### Phase 7. 보호자 알림·F-05 EAS·S-07·S-08 — P1

**목표:** 위험 전이를 보호자 행동과 온체인 증명으로 연결하고 실패 시에도 오프체인 서비스를 유지한다.

#### 1. 알림 outbox와 S-08

- [ ] **P7-01. `NotificationProvider`를 만든다.** `sendGuardianAlert()` 입력과 성공/재시도/영구실패 결과를 도메인 타입으로 고정한다.
- [ ] **P7-02. outbox worker를 만든다.** `guardian_alerts`의 미처리 건을 lock하고 공급자 호출, 응답 ID, 시도 횟수, 다음 시도를 저장한다.
- [ ] **P7-03. 딥링크 토큰을 발급한다.** 256-bit random token을 생성하고 DB에는 hash, 24시간 만료, 1회 교환 상태만 저장한다.
- [ ] **P7-04. 토큰을 세션으로 교환한다.** 첫 유효 요청에서 토큰을 소비하고 24시간 HttpOnly/Secure/SameSite cookie를 발급한다. 새로고침은 세션으로 허용한다.
- [ ] **P7-05. `/alert/$eventId`를 만든다.** 마스킹 이름, HRI, 최대 3개 이유, 지금 할 일, 쉼터 CTA, 119 문구만 반환한다.
- [ ] **P7-06. 만료·재사용을 처리한다.** 만료 또는 이미 다른 브라우저에서 교환한 토큰은 개인정보 없이 안내하고 담당자 문의 경로를 제공한다.

#### 2. 온체인 스키마와 해시

- [ ] **P7-07. EAS 도구 버전을 고정한다.** 현재 Base Sepolia deployment, EAS SDK, viem 호환성을 공식 문서로 확인하고 잠금파일에 고정한다.
- [ ] **P7-08. 두 스키마를 등록한다.** CareEvent와 ShelterStatus schema를 한 번 등록하고 UID, registry transaction, issuer를 배포 설정에 기록한다.
- [ ] **P7-09. schema encoding 테스트를 만든다.** 각 필드 순서·타입·범위와 event enum 0/1/2, risk 0~4를 fixture로 검사한다.
- [ ] **P7-10. 해시를 구현한다.** subject HMAC과 RFC 8785 canonical payload SHA-256을 `hashing.ts`에 두고 동일 입력의 결정성을 테스트한다.
- [ ] **P7-11. 개인정보 차단 테스트를 만든다.** 인코딩 payload에 이름, 전화, 주소, 약품명, subject UUID 원문이 포함되면 테스트를 실패시킨다.

#### 3. 비동기 기록과 재시도

- [ ] **P7-12. EAS adapter를 서버 전용으로 만든다.** RPC, chain ID, signer, schema UID, expected issuer를 시작 시 검증한다.
- [ ] **P7-13. 상태 전이를 구현한다.** DB 이벤트 저장→`PENDING` job→transaction receipt→`VERIFIED`와 UID 저장 순서를 지킨다.
- [ ] **P7-14. 재시도를 구현한다.** retryable 오류는 2s, 8s, 32s 최대 3회 후 `FAILED`로 바꾸고 durable queue에 남긴다. 프로세스 메모리 timer만 사용하지 않는다.
- [ ] **P7-15. 사용자 흐름을 분리한다.** EAS 실패가 shelter report, check-in, alert DB 저장을 롤백하지 않게 한다.
- [ ] **P7-16. Cron/worker를 보호한다.** 중복 job lock, idempotency key, 이미 UID가 있는 이벤트 재발급 금지를 적용한다.

#### 4. S-07 공개 검증

- [ ] **P7-17. `/verify/$uid`를 만든다.** Base Sepolia EAS GraphQL/RPC에서 UID를 조회하고 schema UID, issuer, chain, revoked 상태를 확인한다.
- [ ] **P7-18. 임의 attestation을 거부한다.** UID가 존재해도 온중 schema 또는 expected issuer가 아니면 `온중이 발급한 증명이 아닙니다`로 처리한다.
- [ ] **P7-19. 안전한 필드만 디코드한다.** 유형, 위험 등급, HRI, 발생시각, subject hash, issuer, schema를 보여준다.
- [ ] **P7-20. 복사와 explorer 링크를 제공한다.** 축약 hash를 클릭하면 전체값을 복사하고 `Base Sepolia 테스트넷`과 외부 이동을 표시한다.
- [ ] **P7-21. 없음·폐기·RPC 실패를 구분한다.** not found, revoked, temporary unavailable을 서로 다른 문구와 행동으로 처리한다.

#### 5. 전체 연결

- [ ] **P7-22. ALERT_SENT를 연결한다.** 공급자 성공 후 CareEvent를 만들고 EAS job을 큐에 넣는다.
- [ ] **P7-23. SHELTER_CHECKIN을 연결한다.** 체크인 EAS가 VERIFIED가 되면 HRI 재계산을 큐에 넣는다.
- [ ] **P7-24. ShelterStatus를 연결한다.** S-06 제보 DB 성공 후 별도 EAS job을 만들고 상태 배지를 갱신한다.
- [ ] **P7-25. S-01/S-02 피드를 실제 이벤트로 교체한다.** 최신 10건과 대상자별 이력을 실제 UID·상태로 표시한다.

#### DoD

- L3 진입 1회가 알림 1건, CareEvent 1건, attestation 1건만 만든다.
- 같은 job을 두 worker가 집어도 온체인 중복 발급이 발생하지 않는다.
- RPC가 60초 이상 불가해도 제보·알림·체크인 DB 기록과 사용자 완료 화면은 유지된다.
- S-07은 유효, 없음, 폐기, 타 스키마, 타 발급자, RPC 실패를 구분한다.
- 온체인 raw data에 개인정보 원문이 없다.

---

### Phase 8. 통합 검증·보안·배포·시연 — P0 출시 게이트

**목표:** 4개 핵심 기능이 한 흐름으로 동작하고, 실패·접근성·보안 조건을 포함해 재현 가능한 상태로 배포한다.

#### 1. E2E 시나리오

- [ ] **P8-01. 위험 감지 흐름을 자동화한다.** 기상 fixture 변경→Cron→HRI L3→S-01 경보→알림 outbox→S-08→EAS→S-07을 검증한다.
- [ ] **P8-02. 복약 흐름을 자동화한다.** 촬영 fixture→확인 필요 수정→저장→HRI 전후 변화→S-02 반영을 검증한다.
- [ ] **P8-03. 쉼터 흐름을 자동화한다.** 위치 거부→주소 검색→iM 필터→경로→상태 제보→EAS 상태를 검증한다.
- [ ] **P8-04. 체크인 완화 흐름을 자동화한다.** subject-scoped 세션→체크인→PENDING 동안 C=0→VERIFIED 후 C=6을 검증한다.

#### 2. 실패·복구 테스트

- [ ] **P8-05. 외부 장애 matrix를 실행한다.** KMA, Gemini, MFDS, Naver, TMAP, RPC, 알림 공급자를 하나씩 실패시켜 명세의 폴백과 partial 상태를 확인한다.
- [ ] **P8-06. 새로고침·중복 제출을 검사한다.** 각 mutation에 idempotency를 적용하고 재요청으로 중복 row·알림·attestation이 생기지 않게 한다.
- [ ] **P8-07. 느린 네트워크를 검사한다.** 3G 지연에서 기존 데이터 유지, 단계 로딩, 재시도, 버튼 중복 클릭 차단을 확인한다.

#### 3. 접근성·반응형

- [ ] **P8-08. axe를 실행한다.** 7개 명세 화면과 홈에서 serious/critical 위반 0건을 목표로 한다.
- [ ] **P8-09. 키보드 회귀를 실행한다.** skip link, 필터, table row link, modal, bottom sheet, toast, 복사 버튼을 마우스 없이 사용한다.
- [ ] **P8-10. viewport를 고정 검사한다.** 360, 390, 768, 1024, 1280, 1600px 스크린샷을 비교한다.
- [ ] **P8-11. 저모션·200% 확대를 검사한다.** L4 정보가 사라지지 않고 텍스트·버튼이 겹치지 않는지 확인한다.

#### 4. 보안·데이터 검증

- [ ] **P8-12. Supabase advisor와 RLS 테스트를 실행한다.** 경고를 검토하고 예외는 이유를 문서화한다.
- [ ] **P8-13. secret scan을 실행한다.** git/workspace, client bundle, source map, 로그 fixture에서 키·개인정보 패턴을 검사한다.
- [ ] **P8-14. 공개 URL을 공격 관점에서 검사한다.** IDOR, token 재사용, 임의 UID, report spam, oversized image, malformed JSON을 테스트한다.
- [ ] **P8-15. 데이터 보존 작업을 검증한다.** 약봉투 원본 삭제, 만료 토큰 정리, 오래된 cache 정리, 실패 job 보관정책을 실행한다.

#### 5. 성능·배포

- [ ] **P8-16. 번들을 점검한다.** 지도, Turf, EAS, Gemini 서버 코드를 route/server 경계로 분리하고 초기 시민 번들에 포함하지 않는다.
- [ ] **P8-17. 쿼리 계획을 확인한다.** shelter 반경, 최신 risk, dashboard summary, RLS 필터에 `EXPLAIN ANALYZE`를 실행한다.
- [ ] **P8-18. Vercel preview를 배포한다.** Nitro preset, 환경 변수, Naver 허용 도메인, TMAP 서버 호출, Cron 인증, SSR, deep link를 preview에서 검사한다.
- [ ] **P8-19. 운영 runbook을 작성한다.** 키 교체, Cron 실패, API quota, EAS 잔액, RPC 장애, 데이터 시드 갱신 절차를 `docs/runbook.md`에 기록한다.
- [ ] **P8-20. README를 완성한다.** 로컬 실행, 환경 변수, Supabase reset, 테스트, build, 배포, demo 계정을 한 문서에 기록한다.

#### 6. 시연 자료

- [ ] **P8-21. 결정적 demo seed를 만든다.** 영상 촬영 중 API 변동이 없어도 5개 필수 장면을 재현할 수 있는 fixture mode를 환경 플래그로 제공한다. production에서는 비활성화한다.
- [ ] **P8-22. 명세의 5개 필수 장면을 촬영한다.** 약봉투→+12, HRI 60→72, 보호자→그늘 68%, iM 100곳, EAS 검증 완료를 순서대로 확인한다.
- [ ] **P8-23. 테스트넷과 목데이터를 고지한다.** 영상과 화면에서 Base Sepolia, 가상 대상자, demo fixture 사용 여부를 숨기지 않는다.

#### 최종 DoD

- lint, typecheck, unit, component, pgTAP, E2E, build가 모두 통과한다.
- 7개 명세 라우트가 존재하고 권한·토큰 정책대로 접근된다.
- F-01/F-02/F-04/F-05의 정상 흐름과 필수 실패 폴백이 자동 테스트로 재현된다.
- 실제 개인정보와 secret이 저장소, 번들, 로그, 온체인에 없다.
- preview 배포에서 Cron, deep link, Naver 허용 도메인, TMAP 보행 경로, EAS Base Sepolia 검증이 동작한다.

## 7. 우선순위와 의존 순서 요약

| 순서 | Phase                       | 우선순위 | 선행조건              | 주 산출물                           |
| ---: | --------------------------- | -------- | --------------------- | ----------------------------------- |
|    1 | Phase 0 명세·기준선         | P0       | 없음                  | 확정된 데이터 소스·공급자·도구 체계 |
|    2 | Phase 1 도메인·UI 기반      | P0       | Phase 0               | 검증된 HRI, 공통 상태/UI            |
|    3 | Phase 2 Supabase·Auth·시드  | P0       | Phase 1               | 안전한 DB, 950개 쉼터, 가상 대상자  |
|    4 | Phase 3 기상·배치·S-01/S-02 | P0       | Phase 2               | 실제 F-01 end-to-end                |
|    5 | Phase 4 복약·S-03           | P1       | Phase 3               | F-02 end-to-end                     |
|    6 | Phase 5 쉼터·S-05/S-06      | P1       | Phase 2               | 실제 지도와 상태 제보               |
|    7 | Phase 6 그늘길              | P1       | Phase 5 + 공간 데이터 | F-04 end-to-end                     |
|    8 | Phase 7 알림·EAS·S-07/S-08  | P1       | Phase 3, 5, 6         | F-05와 보호자 흐름                  |
|    9 | Phase 8 출시 게이트         | P0       | 전 phase              | 배포, 검증, 시연                    |

Phase 4와 Phase 5는 Phase 3 완료 후 서로 다른 파일 소유권으로 병렬 진행할 수 있다. Phase 6은 Phase 5의 실제 쉼터·지도, Phase 7은 Phase 3의 전이 이벤트와 Phase 5/6의 제보·체크인에 의존한다.

## 8. 구현 중 변경 금지 기준

- HRI 숫자와 등급 임계값을 UI 편의를 위해 바꾸지 않는다.
- 운영상태 제보를 대상자 체크인으로 간주하지 않는다.
- 기상 실패 시 E=0을 저장하지 않는다.
- AI 판독 실패가 수동 입력을 막지 않게 한다.
- EAS 실패가 오프체인 저장과 사용자 완료를 막지 않게 한다.
- 개인정보와 EAS signer key를 브라우저 코드로 보내지 않는다.
- 공개 S-07에서 임의 EAS UID를 온중 증명으로 표시하지 않는다.
- 지도의 정보를 목록보다 더 많이 제공하지 않는다.
- `routeTree.gen.ts`를 수동 편집하지 않는다.
- 외부 API 응답을 타입 단언만으로 신뢰하지 않는다.

## 9. 남아 있는 외부 의존 위험

다음 항목은 코딩만으로 해결할 수 없다. Phase 0에서 실제 계정·데이터·승인을 확보해야 한다.

1. KMA 500m 체감온도 API 사용 권한과 실제 호출 주기
2. 계단·5% 경사·휴식 지점을 증명할 공간 데이터와 라이선스
3. 건축물 높이 데이터의 누락률과 갱신일
4. SMS/알림톡 공급자, 발신 프로필, 템플릿 승인
5. Naver Reverse Geocoding 권한과 Naver/TMAP quota·과금·허용 도메인/IP 설정
6. Base Sepolia RPC, signer test ETH, EAS schema 등록 권한
7. 대상자 개인정보 처리방침과 실제 서비스 동의 절차
8. 2020년 쉼터 데이터의 폐점·이전 표본 검증

이 중 2번이 해결되지 않으면 F-04의 “계단 절대 배제·경사 5% 초과 배제” 완료 기준을 충족할 수 없다. 4번이 해결되지 않으면 보호자 알림은 demo provider까지만 완료할 수 있다.

## 10. 공식 문서 확인 링크

- KMA 대상·환경별 체감온도 종료 안내: <https://www.weather.go.kr/w/forecast/life/composite-index.do>
- KMA 생활기상지수 4.0: <https://www.data.go.kr/tcs/dss/selectApiDataDetailView.do?publicDataPk=15085288>
- Naver Maps 개요: <https://guide.ncloud-docs.com/docs/maps-overview>
- Naver Maps JS API v3: <https://navermaps.github.io/maps.js.ncp/docs/tutorial-2-Getting-Started.html>
- TMAP API 가이드: <https://tmapapi.tmapmobility.com/main.html>
- Gemini 3.5 Flash 모델: <https://ai.google.dev/gemini-api/docs/models/gemini-3.5-flash>
- Gemini Structured Output: <https://ai.google.dev/gemini-api/docs/structured-output>
- Supabase RLS: <https://supabase.com/docs/guides/database/postgres/row-level-security>
- Supabase breaking changes: <https://supabase.com/changelog?types=breaking-change>
- EAS Base Sepolia GraphQL: <https://docs.attest.org/docs/developer-tools/api>
- EAS deployment artifacts: <https://github.com/ethereum-attestation-service/eas-contracts>
- TanStack Start hosting: <https://tanstack.com/start/latest/docs/framework/react/guide/hosting>

## 11. 최종 인수 체크리스트

- [ ] 실제 라우트가 `/dashboard`, `/subjects/$id`, `/medication/$subjectId`, `/shelters`, `/report/$shelterId`, `/verify/$uid`, `/alert/$eventId`에 존재한다.
- [ ] 대시보드와 홈에서 `src/lib/mock/data.ts`를 import하지 않는다.
- [ ] 쉼터 950건, iM 100건, 8개 구·군 집계가 자동 검증된다.
- [ ] HRI의 모든 경계, 상한, 중복 계열, C−6 규칙이 단위 테스트로 고정돼 있다.
- [ ] KMA 실패, Gemini 실패, 위치/카메라 거부, EAS 실패를 수동 경로 또는 캐시로 복구한다.
- [ ] 지도 없이도 동일한 조회·정렬·선택·제보가 가능하다.
- [ ] RLS와 GRANT가 조직·담당자·공개 사용자 경계를 강제한다.
- [ ] 공개 토큰과 증명 페이지가 개인정보 원문을 반환하지 않는다.
- [ ] 모든 오류 문구가 문제와 다음 행동을 함께 설명한다.
- [ ] 360~1600px, 200% 확대, reduced-motion, 키보드 동작을 검증한다.
- [ ] preview 배포에서 Cron, Supabase, Naver, TMAP, Gemini, MFDS, EAS를 실제 계정으로 smoke test한다.
- [ ] README와 runbook만으로 새 환경을 재현할 수 있다.

# 온중(溫證) 서비스 상세 설계서

> **문서 성격** — 구현 기준 문서. 개발자·디자이너가 별도 해석 없이 그대로 구현할 수 있도록 수치·규칙·조건·예시로 기술한다. 본 문서에 없는 값은 임의로 정하지 말고 문서를 갱신한 뒤 구현한다.
>
> | 항목            | 값                                           |
> | --------------- | -------------------------------------------- |
> | 문서 버전       | v1.2                                         |
> | 대상 대회       | 2026 AI Blockchain Challenge in Daegu        |
> | 구현 범위       | Tier 0-② 위험도 엔진 + Tier 1 전체(3종)      |
> | 개발 기간       | 4주 / 1인                                    |
> | 적용 가이드라인 | Vercel Web Interface Guidelines, WCAG 2.2 AA |

---

## 목차

1. [제품 정의와 범위](#1-제품-정의와-범위)
2. [시스템 아키텍처](#2-시스템-아키텍처)
3. [핵심 엔진 명세](#3-핵심-엔진-명세)
4. [데이터 모델](#4-데이터-모델)
5. [외부 연동 명세](#5-외부-연동-명세)
6. [디자인 시스템](#6-디자인-시스템)
7. [레이아웃 규칙](#7-레이아웃-규칙)
8. [공통 컴포넌트 규격](#8-공통-컴포넌트-규격)
9. [화면별 상세 명세](#9-화면별-상세-명세)
10. [사용자 흐름](#10-사용자-흐름)
11. [상태값 정의](#11-상태값-정의)
12. [반응형 기준](#12-반응형-기준)
13. [접근성 원칙](#13-접근성-원칙)
14. [예외 처리 기준](#14-예외-처리-기준)
15. [구현 순서](#15-구현-순서)

---

## 1. 제품 정의와 범위

### 1.1 한 줄 정의

> 대구의 폭염 취약 어르신에 대해 **누가 위험한지 예측하고**, **어디로 가야 하는지 안내하고**, **그 돌봄이 실제로 있었는지 증명하는** 서비스.

### 1.2 경쟁 포지셔닝 (설계 제약으로 작용)

네이버 클로바 케어콜이 전국 시군구 절반 이상(대구 포함)에서 AI 안부전화를 이미 운영 중이다. 따라서 **본 서비스는 "AI 안부전화"로 포지셔닝하지 않는다.**

| 구분      | 클로바 케어콜         | 온중                                     |
| --------- | --------------------- | ---------------------------------------- |
| 핵심      | 대화·정서 교감        | **위험 예측 → 대피 실행 → 증명**         |
| 위험 판단 | 발화 텍스트 감성 분석 | **환경·복약·개인 조건 기반 위험도 계산** |
| 개인 변수 | 대화 이력             | **복약 정보 기반 생리학적 위험도**       |
| 사후 행동 | 담당자 알림           | **그늘 경로 + 실시간 개방 쉼터 라우팅**  |
| 기록      | 내부 DB               | **온체인 증명(위변조 불가)**             |

> **설계 원칙 D-1.** 어떤 화면에서도 "AI와 대화하기"를 1차 가치로 노출하지 않는다. 1차 가치는 항상 **위험도 수치**다.

### 1.3 구현 범위 (확정)

| ID   | 기능                                       | Tier | 포함 |
| ---- | ------------------------------------------ | ---- | ---- |
| F-01 | 개인별 폭염 위험도 엔진 (HRI)              | 0    | ✅   |
| F-02 | 약봉투 촬영 → 폭염 위험 약물 판별          | 1    | ✅   |
| F-04 | 그늘길 기반 쉼터 라우팅                    | 1    | ✅   |
| F-05 | 쉼터 실시간 운영상태 크라우드소싱 + 온체인 | 1    | ✅   |

### 1.4 사용자 정의

| 코드    | 페르소나                      | 주 디바이스          | 사용 표면           |
| ------- | ----------------------------- | -------------------- | ------------------- |
| **U-A** | 생활지원사 / 지자체 돌봄 담당 | 데스크톱 1440px      | Shade (다크)        |
| **U-B** | 보호자 (자녀 세대)            | 모바일 390px         | Paper (라이트)      |
| **U-C** | 어르신 본인                   | 모바일 390px, 저시력 | Paper + 시니어 모드 |
| **U-D** | 검증자 (심사위원·감사·iM뱅크) | 데스크톱/모바일      | Paper (공개)        |

---

## 2. 시스템 아키텍처

### 2.1 기술 스택 (확정)

| 레이어              | 선택                                        | 버전/비고                                                            |
| ------------------- | ------------------------------------------- | -------------------------------------------------------------------- |
| 프레임워크          | TanStack Start + Vite + React               | Start 1.168.x, Router 1.170.x, Vite 8.x, React 19, TypeScript strict |
| 스타일              | Tailwind CSS + CSS Variables                | 토큰은 CSS Var, 유틸은 Tailwind                                      |
| DB / Auth / Storage | Supabase (PostgreSQL + PostGIS)             | PostGIS 필수 (공간 질의)                                             |
| AI                  | Google Gemini API                           | `gemini-3.5-flash` (약봉투 판독)                                     |
| 블록체인            | EAS on Base Sepolia (테스트넷)              | `@ethereum-attestation-service/eas-sdk` + `viem`                     |
| 지도·보행           | Naver Maps JS API v3 + TMAP 보행자 경로안내 | 마커·경로 폴리라인                                                   |
| 태양 위치           | `suncalc`                                   | 그림자 계산                                                          |
| 공간 연산           | `@turf/turf`                                | 폴리곤·버퍼·교차                                                     |
| 스케줄러            | Vercel Cron                                 | 위험도 배치 재계산                                                   |
| 배포                | Vercel                                      | —                                                                    |

### 2.2 디렉터리 구조

```
/src
  /routes                  # TanStack Router 파일 기반 라우트
    __root.tsx             # 전역 셸·메타·오류 경계
    index.tsx              # S-00 랜딩
    dashboard.tsx          # S-01 /dashboard (Shade)
    subjects.$id.tsx       # S-02 /subjects/:id (Shade)
    shelters.tsx           # S-05 /shelters (Paper)
    report.$shelterId.tsx  # S-06 /report/:shelterId (Paper)
    alert.$eventId.tsx     # S-08 /alert/:eventId (Paper)
    verify.$uid.tsx        # S-07 /verify/:uid (공개)
    medication.$subjectId.tsx # S-03 /medication/:subjectId
  /components
    /shade                 # 관제 표면 전용 컴포넌트
    /paper                 # 시민 표면 전용 컴포넌트
    /onjung                # 공통 도메인 컴포넌트
    /ui                    # 공통 UI 프리미티브
  /lib
    /risk/hri.ts           # F-01 위험도 엔진
    /gemini/schemas.ts     # Structured Output 스키마
    /gemini/med-reader.ts  # F-02
    /route/shade.ts        # F-04 그늘 경로
    /chain/eas.ts          # F-05 온체인
    /external/*.ts         # 공공 API 클라이언트
/data
  daegu_shelters.geojson   # 950건 (전처리 완료)
```

### 2.3 데이터 흐름

```
[Vercel Cron 30분]
      │
      ├─→ APIHub 500m 관측 (대상자 위치별 캐시) ─┐
      ├─→ 무더위쉼터 마스터 (950건, 정적) ─┤
      │                                    ▼
      │                            ┌──────────────┐
      │                            │  HRI 엔진    │←── 복약 프로필 (F-02)
      │                            │  (F-01)      │
      │                            └──────┬───────┘
      │                                   ▼
      │                            risk_snapshots 갱신
      │                                   │
      │              ┌────────────────────┼────────────────────┐
      ▼              ▼                    ▼                    ▼
  S-01 대시보드   L3↑ 알림 발송      S-05 쉼터 라우팅     EAS 기록 (F-05)
```

---

## 3. 핵심 엔진 명세

### 3.1 HRI (Heat Risk Index) — F-01

**정의.** 0–100 정수. 대상자 1인의 현재 온열질환 위험도.

```
HRI = clamp(0, 100, E + M + P − C)
```

#### 3.1.1 E — 환경 점수 (0–50)

체감온도 `T`는 기상청 API허브 고해상도 500m 특정지점 다중요소 조회의 일반 체감온도 `ta_chi`를 1차 값으로 사용한다. 같은 응답의 `ta`·`hm`도 함께 저장한다. 미수신 시 공공데이터포털 단기예보 `TMP`+`REH`로 Stull 근사식 대체(§14.2 참조).

| 체감온도 T (℃) | 기본 점수 |
| -------------- | --------- |
| T < 31         | 0         |
| 31 ≤ T < 33    | 10        |
| 33 ≤ T < 35    | 20        |
| 35 ≤ T < 38    | 32        |
| 38 ≤ T < 40    | 42        |
| T ≥ 40         | 50        |

**가산** (E 합계 상한 50)

- 폭염주의보 발효 중: `+3`
- 폭염경보 발효 중: `+5`
- 열대야 3일 연속 이상: `+5`

#### 3.1.2 M — 복약 점수 (0–25)

질병관리청이 제시한 폭염 주의 의약품 13개 계열을 2단계로 구분한다.

| 위험 등급  | 계열                                                                                                       | 계열당 점수 |
| ---------- | ---------------------------------------------------------------------------------------------------------- | ----------- |
| **고위험** | 이뇨제, 항콜린제, 항정신병제, 항우울제, 1세대 항히스타민제                                                 | `+6`        |
| **중위험** | 혈압강하제, 칼슘채널길항제, 질산염·혈관확장제, 리튬, 항간질제, 항치매제, 항불안제·근이완제, 교감신경흥분제 | `+3`        |

- 동일 계열 중복 복용은 1회만 계산한다.
- 합계 상한 `25`.
- 복약 정보 미등록 대상자는 `M = 0`이며, UI에 **"복약 정보 미등록 — 위험도가 과소평가될 수 있습니다"** 경고 배지를 표시한다.

#### 3.1.3 P — 개인 점수 (0–20)

| 조건                                         | 점수 |
| -------------------------------------------- | ---- |
| 85세 이상                                    | `+8` |
| 75–84세                                      | `+5` |
| 65–74세                                      | `+3` |
| 독거                                         | `+5` |
| 만성질환 보유 (심혈관·신장·당뇨 중 1개 이상) | `+4` |
| 냉방기 없음 또는 미가동                      | `+3` |

합계 상한 `20`.

#### 3.1.4 C — 완화 점수 (0–6, 차감)

| 조건                                                 | 차감 |
| ---------------------------------------------------- | ---- |
| 계산 시각 기준 최근 24시간 내 `VERIFIED` 쉼터 체크인 | `−6` |

> **규칙 E-1.** C−6의 유일한 근거는 `shelter_checkins`에서 `checked_in_at >= computedAt - 24시간`이고 `attestation_state = 'VERIFIED'`인 기록이다. `shelter_reports`의 공개 운영상태 제보, `PENDING`·`FAILED`·`UNVERIFIED` 체크인, 24시간이 지난 체크인은 차감하지 않는다. 체크인 직후에는 C=0을 유지하고, EAS 검증 완료 후 다음 HRI 재계산부터 C=6을 적용한다.

#### 3.1.5 등급 구간과 자동 조치

| 등급 | HRI    | 라벨 | 자동 조치                              |
| ---- | ------ | ---- | -------------------------------------- |
| `L0` | 0–19   | 안전 | 없음                                   |
| `L1` | 20–39  | 관심 | 대시보드 표시만                        |
| `L2` | 40–59  | 주의 | 대시보드에서 주의 대상 강조            |
| `L3` | 60–79  | 경고 | 보호자 알림 + 쉼터 경로 발송           |
| `L4` | 80–100 | 위험 | 담당자 즉시 알림 + 응급 연계 안내 표시 |

#### 3.1.6 L3/L4 알림 전이와 중복 방지

알림은 30분 배치마다 반복 발송하지 않고, 대상자별 **위험 에피소드**와 등급 전이를 기준으로 생성한다.

| 조건                              | 알림 규칙                                                                        |
| --------------------------------- | -------------------------------------------------------------------------------- |
| `L0`~`L2`에서 `L3` 또는 `L4` 진입 | 해당 등급 진입 알림 1회                                                          |
| `L3 → L4` 악화                    | 기존 L3 알림 시각과 무관하게 L4 알림 즉시 1회                                    |
| 동일한 `L3` 또는 `L4` 지속        | 최초 알림 2시간 후 지속 알림 1회만 발송. 이후 같은 에피소드에서는 추가 반복 금지 |
| `L0`~`L2`로 회복                  | 현재 위험 에피소드를 종료하고 지속 타이머를 폐기                                 |
| 회복 후 `L3` 또는 `L4` 재진입     | 새 위험 에피소드와 새 진입 이벤트로 처리                                         |

- 위험 진입 시 서버가 `episodeId`를 한 번 생성한다. idempotency key는 `subjectId:episodeId:riskLevel:triggerKind` 형식으로 만들며 `triggerKind`는 `ENTER`, `ESCALATE`, `PERSIST_2H` 중 하나다. 회복 후 재진입은 새 `episodeId`를 사용하므로 같은 2시간 구간 안에서도 새 이벤트가 되며, 같은 에피소드의 반복 실행은 같은 키를 재사용한다.
- `alert_transitions.idempotency_key`에 UNIQUE 제약을 적용하고, 알림 공급자 요청과 `ALERT_SENT` EAS 작업에도 같은 키를 전달한다. Cron 재실행·네트워크 재시도·동시 worker가 동일한 키를 다시 처리해도 DB 행, 외부 알림, attestation을 중복 생성하지 않는다.
- `L4 → L3`은 악화가 아니므로 새 즉시 알림을 만들지 않는다. 동일 에피소드의 2시간 지속 알림 조건만 평가한다.

#### 3.1.7 참조 구현

```ts
// lib/risk/hri.ts
export type RiskLevel = "L0" | "L1" | "L2" | "L3" | "L4";

export interface HriInput {
  feelsLikeC: number;
  heatAdvisory: "NONE" | "WATCH" | "WARNING";
  tropicalNightStreak: number;
  medHigh: number; // 고위험 계열 수 (중복 제거)
  medMid: number; // 중위험 계열 수
  medRegistered: boolean;
  age: number;
  livesAlone: boolean;
  chronicDisease: boolean;
  noCooling: boolean;
  shelterCheckInVerified24h: boolean;
}

export interface HriResult {
  score: number;
  level: RiskLevel;
  breakdown: { E: number; M: number; P: number; C: number };
  reasons: string[]; // UI 노출용, 기여도 내림차순 최대 3개
}

const clamp = (min: number, max: number, v: number) => Math.min(max, Math.max(min, v));

export function computeHri(i: HriInput): HriResult {
  // E
  const t = i.feelsLikeC;
  let E = t >= 40 ? 50 : t >= 38 ? 42 : t >= 35 ? 32 : t >= 33 ? 20 : t >= 31 ? 10 : 0;
  if (i.heatAdvisory === "WARNING") E += 5;
  else if (i.heatAdvisory === "WATCH") E += 3;
  if (i.tropicalNightStreak >= 3) E += 5;
  E = clamp(0, 50, E);

  // M
  const M = i.medRegistered ? clamp(0, 25, i.medHigh * 6 + i.medMid * 3) : 0;

  // P
  let P = i.age >= 85 ? 8 : i.age >= 75 ? 5 : i.age >= 65 ? 3 : 0;
  if (i.livesAlone) P += 5;
  if (i.chronicDisease) P += 4;
  if (i.noCooling) P += 3;
  P = clamp(0, 20, P);

  // C
  const C = i.shelterCheckInVerified24h ? 6 : 0;

  const score = clamp(0, 100, E + M + P - C);
  const level: RiskLevel =
    score >= 80 ? "L4" : score >= 60 ? "L3" : score >= 40 ? "L2" : score >= 20 ? "L1" : "L0";

  return { score, level, breakdown: { E, M, P, C }, reasons: buildReasons(i, { E, M, P, C }) };
}
```

> **규칙 E-2.** `reasons`는 반드시 사람이 읽을 수 있는 한 문장으로 생성한다. 예: `"이뇨제·항콜린제 복용 중 (+12)"`, `"체감 39.2℃ + 폭염경보 (+47)"`. 화면에는 최대 3개만 노출한다.

### 3.2 약봉투 판독 — F-02

**파이프라인**

```
사진 촬영
  → Gemini 멀티모달 판독 (제품명·모양·각인·색상 추출)
  → 식약처 낱알식별 API 조회 (모양/색/각인 → 품목기준코드)
  → e약은요 API 조회 (효능·주의사항·상호작용)
  → DUR 품목정보 조회 (노인주의·병용금기)
  → 13개 계열 매칭 → M 점수 산출
```

**Gemini Structured Output 스키마**

```ts
// lib/gemini/schemas.ts
export const MedicationExtractionSchema = {
  type: "object",
  required: ["items", "imageQuality"],
  properties: {
    imageQuality: {
      type: "string",
      enum: ["GOOD", "BLURRY", "PARTIAL", "UNREADABLE"],
    },
    items: {
      type: "array",
      items: {
        type: "object",
        required: ["rawText", "confidence"],
        properties: {
          rawText: { type: "string" }, // 봉투/알약에서 읽은 원문
          productName: { type: "string" },
          shape: { type: "string", enum: ["원형", "타원형", "장방형", "삼각형", "기타", "불명"] },
          color: { type: "string" },
          imprint: { type: "string" }, // 각인
          dosageText: { type: "string" }, // "1일 3회 식후 30분"
          confidence: { type: "number" }, // 0.0–1.0
        },
      },
    },
  },
} as const;
```

**신뢰도 처리 규칙**

| `confidence`           | 처리                                                      |
| ---------------------- | --------------------------------------------------------- |
| ≥ 0.85                 | 자동 확정, 배지 `자동 인식`                               |
| 0.60 – 0.84            | 사용자 확인 필요, 배지 `확인 필요`, 수정 가능 입력창 노출 |
| < 0.60                 | 자동 확정 금지. `수동 입력` 폼으로 전환                   |
| `imageQuality != GOOD` | 재촬영 유도 화면 우선 노출 (§14.4)                        |

> **규칙 F2-1.** 판독 결과는 **의학적 조언이 아니다.** 모든 결과 화면 하단에 고정 문구를 표시한다: `이 정보는 폭염 위험도 계산용 참고 자료입니다. 복약 변경은 반드시 의사·약사와 상의하세요.`

### 3.3 시연용 그늘·접근성 우선 경로 후보 — F-04

```
1. 출발지(대상자 좌표) + 도착지(추천 쉼터)
2. TMAP 보행자 경로 `searchOption=30` → GeoJSON 폴리라인 좌표열, Naver 지도에 표시
3. suncalc로 현재 시각 태양 고도(altitude)·방위(azimuth) 계산
4. 경로 주변 30m 내 건물(건축물대장 높이) → 그림자 폴리곤 생성
   shadowLength = height / tan(altitude)
5. turf.booleanPointInPolygon으로 경로 세그먼트별 그늘 여부 판정
6. shadeRatio = 그늘 구간 길이 / 전체 길이
```

**노인 보행 파라미터 (필수 적용)**

| 항목           | 값                                                                      |
| -------------- | ----------------------------------------------------------------------- |
| 보행 속도      | `0.75 m/s` (일반 성인 1.3 m/s 대비)                                     |
| 목표 휴식 간격 | 확인 가능한 공원시설 기준 `300 m`; 도시 전역 충족은 보장하지 않음       |
| 경사 위험 임계 | DEM 파생 `5% 초과` 확인 구간 회피; 실제 보행면 경사는 현장 확인 필요    |
| 계단           | TMAP 계단 제외 옵션 + 확인된 OSM 계단 회피; 미등록 계단은 보장하지 않음 |

**경로 스코어**

```
routeScore = shadeRatio × 0.6
           + (1 − normalize(durationSec)) × 0.25
           + restSpotDensity × 0.15
```

- 후보 경로 최대 3개를 생성해 `routeScore` 내림차순 정렬한다.
- 태양 고도 `altitude ≤ 0` (일몰 후)이면 그늘 계산을 건너뛰고 **최단 경로**로 대체하며, UI에 `일몰 후 — 최단 경로로 안내합니다` 배너를 표시한다.
- 모든 후보에는 `시연용 접근성 우선 후보` 배지와 `TMAP 계단 제외 옵션과 공개 공간자료를 반영한 후보입니다. 미등록 계단·급경사·휴식시설 운영 여부는 보장하지 않으므로 이동 전 현장을 확인하세요.` 안내를 표시한다. `안전 경로`, `무계단 보장`, `경사 5% 이하`, `300m마다 휴식 가능` 표현은 사용하지 않는다.
- 데이터 공급처·좌표계·라이선스·갱신 기준과 불확실성 규칙은 `docs/route-data-contract.md`를 따른다.

### 3.4 온체인 증명 — F-05

**체인.** Base Sepolia (테스트넷). **프로토콜.** EAS.

**스키마 2종**

```solidity
// SCHEMA-A: CareEvent — 돌봄 수행 증명
bytes32 subjectHash    // 서버 비밀키 HMAC-SHA-256 (원본 식별자 미기록)
uint8   eventType      // 0=VISIT 1=SHELTER_CHECKIN 2=ALERT_SENT
uint8   riskLevel      // 0..4 (L0..L4)
uint16  hriScore       // 0..100
uint64  occurredAt     // Unix seconds
bytes32 payloadHash    // 개인정보 제거 JSON → RFC 8785 정규화 → SHA-256
```

```solidity
// SCHEMA-B: ShelterStatus — 쉼터 운영상태 제보
string  shelterId      // "DG-0001"
bool    isOpen
uint8   crowdLevel     // 0=여유 1=보통 2=혼잡
uint64  observedAt
bytes32 reporterHash   // 익명 세션 식별자의 서버 HMAC-SHA-256
```

> **규칙 F5-1.** 개인정보는 원문뿐 아니라 개인정보가 든 JSON·이미지·URL·메타데이터 형태로도 **절대 온체인에 기록하지 않는다.** 이름·전화번호·주소·생년·복약명·내부 `subject_id`는 오프체인 DB에만 저장한다.
>
> **규칙 F5-2.** `subjectHash = HMAC-SHA-256(SUBJECT_HASH_SECRET, subjectId)`로 서버에서만 생성한다. 브라우저, 응답 DTO, 로그에 원본 식별자·서버 비밀을 노출하지 않으며 일반 SHA-256으로 대체하지 않는다.
>
> **규칙 F5-3.** `payloadHash` 대상은 허용 목록으로 만든 개인정보 비포함 이벤트 JSON이다. RFC 8785 호환 구현으로 canonical JSON을 만든 뒤 UTF-8 바이트의 SHA-256을 계산한다. 키 정렬만 한 임의 JSON 문자열화나 원본 요청 body 해시는 허용하지 않는다.
>
> **규칙 F5-4.** EAS 상태·UID·해시·Explorer 링크·기록 액션을 표시하는 모든 화면과 컴포넌트에는 축약하지 않은 문구 `Base Sepolia 테스트넷`을 항상 함께 표시한다. 다른 네트워크의 기록을 성공으로 렌더링하지 않는다.
>
> **규칙 F5-5.** 블록체인이 필요한 이유는 화면에 명시한다 — `이 기록은 Base Sepolia 테스트넷에 발급되며, 발급·폐기 이력을 공개 검증할 수 있습니다. 운영 결제나 실물 자산 거래가 아닙니다.`

---

## 4. 데이터 모델

### 4.1 테이블 정의

```sql
-- 대상자
CREATE TABLE subjects (
  id              uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  name            text NOT NULL,
  birth_year      smallint NOT NULL,
  phone           text,
  guardian_phone  text,
  address         text NOT NULL,
  location        geography(Point, 4326) NOT NULL,
  kma_nx          smallint NOT NULL,
  kma_ny          smallint NOT NULL,
  lives_alone     boolean NOT NULL DEFAULT true,
  chronic_disease boolean NOT NULL DEFAULT false,
  has_cooling     boolean NOT NULL DEFAULT true,
  senior_mode     boolean NOT NULL DEFAULT false,
  consented_at    timestamptz NOT NULL,   -- 개인정보 동의 시각 (필수)
  created_at      timestamptz NOT NULL DEFAULT now()
);

-- 쉼터 마스터 (950건, daegu_shelters.geojson에서 시드)
CREATE TABLE shelters (
  id             text PRIMARY KEY,        -- "DG-0001"
  name           text NOT NULL,
  gu             text NOT NULL,
  facility_type  text NOT NULL,           -- 경로당|금융기관|행정복지센터|...
  is_im_bank     boolean NOT NULL DEFAULT false,
  road_address   text NOT NULL,
  location       geography(Point, 4326) NOT NULL,
  kma_nx         smallint NOT NULL,
  kma_ny         smallint NOT NULL
);
CREATE INDEX idx_shelters_loc ON shelters USING GIST (location);

-- 공개 쉼터 운영상태 제보. subject_id를 추가하거나 체크인으로 해석하지 않는다.
CREATE TABLE shelter_reports (
  id               uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  shelter_id       text NOT NULL REFERENCES shelters(id),
  is_open          boolean NOT NULL,
  crowd_level      smallint NOT NULL DEFAULT 1,
  observed_at      timestamptz NOT NULL,
  reporter_hash    text NOT NULL,
  attestation_state text NOT NULL DEFAULT 'UNVERIFIED'
                    CHECK (attestation_state IN ('UNVERIFIED', 'PENDING', 'VERIFIED', 'FAILED')),
  attestation_uid  text,                  -- EAS UID, NULL이면 미검증
  created_at       timestamptz NOT NULL DEFAULT now()
);

-- 대상자 쉼터 체크인. 인증 담당자 또는 서버가 검증한 subject-scoped 세션만 생성한다.
CREATE TABLE shelter_checkins (
  id                uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  subject_id        uuid NOT NULL REFERENCES subjects(id) ON DELETE CASCADE,
  shelter_id        text NOT NULL REFERENCES shelters(id),
  checked_in_at     timestamptz NOT NULL,
  actor_scope       text NOT NULL CHECK (actor_scope IN ('CAREGIVER', 'SUBJECT_SCOPED')),
  actor_ref_hash    text NOT NULL,        -- 서버에서 인증 주체를 HMAC 처리한 참조값
  attestation_state text NOT NULL DEFAULT 'UNVERIFIED'
                    CHECK (attestation_state IN ('UNVERIFIED', 'PENDING', 'VERIFIED', 'FAILED')),
  attestation_uid   text,
  created_at        timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_checkins_subject_time
  ON shelter_checkins (subject_id, checked_in_at DESC)
  WHERE attestation_state = 'VERIFIED';

-- 복약 프로필
CREATE TABLE medications (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  subject_id    uuid NOT NULL REFERENCES subjects(id) ON DELETE CASCADE,
  product_name  text NOT NULL,
  item_seq      text,                     -- 식약처 품목기준코드
  heat_class    text,                     -- 이뇨제|항콜린제|... NULL이면 비해당
  risk_tier     text NOT NULL,            -- HIGH|MID|NONE
  source        text NOT NULL,            -- AI_AUTO|AI_CONFIRMED|MANUAL
  confidence    real,
  created_at    timestamptz NOT NULL DEFAULT now()
);

-- 위험도 스냅샷 (30분 배치)
CREATE TABLE risk_snapshots (
  id           bigserial PRIMARY KEY,
  subject_id   uuid NOT NULL REFERENCES subjects(id) ON DELETE CASCADE,
  hri          smallint NOT NULL,
  level        text NOT NULL,
  breakdown    jsonb NOT NULL,
  reasons      text[] NOT NULL,
  computed_at  timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_risk_subject_time ON risk_snapshots (subject_id, computed_at DESC);

-- L3/L4 위험 에피소드의 알림 가능 전이. 같은 key는 한 번만 처리한다.
CREATE TABLE alert_transitions (
  id                uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  subject_id        uuid NOT NULL REFERENCES subjects(id) ON DELETE CASCADE,
  episode_id        uuid NOT NULL,
  episode_started_at timestamptz NOT NULL,
  from_level        text NOT NULL,
  to_level          text NOT NULL,
  transition_type   text NOT NULL
                    CHECK (transition_type IN ('ENTER', 'ESCALATE', 'PERSIST_2H', 'REENTER')),
  idempotency_key   text NOT NULL UNIQUE,
  occurred_at       timestamptz NOT NULL DEFAULT now()
);
```

`shelter_reports`는 공개 사용자가 서버 액션으로 제출하는 운영상태·혼잡도 자료이며 공개용 안전 DTO로만 조회한다. `shelter_checkins`는 인증된 담당자 또는 해당 대상자에 한정된 서버 검증 세션만 생성할 수 있다. 두 테이블 사이에 복사·자동 승격 트리거를 두지 않으며, 공개 제보 성공이나 `ShelterStatus` 검증 성공은 HRI C에 영향을 주지 않는다.

### 4.2 쉼터 시드 데이터 (전처리 완료)

`daegu_shelters.geojson` — 950건, 8개 구·군.

| 유형         | 건수 | 비율  |
| ------------ | ---- | ----- |
| 경로당       | 466  | 49.1% |
| 금융기관     | 245  | 25.8% |
| 행정복지센터 | 129  | 13.6% |
| 기타         | 110  | 11.5% |

> **활용 규칙 S-1.** 금융기관 245곳 중 **iM뱅크(대구은행) 지점이 100곳**이다. `is_im_bank = true` 마커는 지도에서 전용 색상으로 구분하고, 필터 칩 `iM뱅크 쉼터`를 기본 제공한다. 이는 사업화 서사의 핵심 근거이므로 UI에서 반드시 가시화한다.

---

## 5. 외부 연동 명세

| #   | API                                            | 용도                            | 호출 주기                 | 인증                            |
| --- | ---------------------------------------------- | ------------------------------- | ------------------------- | ------------------------------- |
| 1   | 기상청 API허브 500m 특정지점 다중요소          | `ta_chi`·`ta`·`hm` (1차)        | 30분 / 대상자 위치별 캐시 | API허브 `authKey`               |
| 2   | 기상청 단기예보 `getVilageFcst`                | `TMP`·`REH` (2차 폴백)          | 30분 / 5km 격자별 캐시    | 공공데이터포털 키               |
| 3   | 기상청 API허브 특보현황 `wrn_now_data_new.php` | 폭염주의보·경보                 | 30분 / 전역 1회           | API허브 `authKey`               |
| 4   | 식약처 의약품 낱알식별                         | 알약 → 품목코드                 | 요청 시                   | 동일                            |
| 5   | 식약처 e약은요                                 | 효능·주의사항                   | 요청 시                   | 동일                            |
| 6   | 식약처 DUR 품목정보                            | 노인주의·병용금기               | 요청 시                   | 동일                            |
| 7   | 국토부 GIS건물통합정보                         | footprint·높이 후보 (그림자)    | 데이터셋 갱신 시          | 공공데이터 이용조건 확인        |
| 8   | 국토지리정보원 DEM + OSM 계단                  | 경사 위험·확인된 계단 보조 필터 | snapshot 갱신 시          | 공공데이터 조건 + ODbL 표시     |
| 9   | 대구 공원시설물                                | 벤치·정자 휴식 후보             | 1일 캐시                  | 공공데이터포털 키               |
| 10  | Naver Maps                                     | 지도·주소 검색                  | 요청 시                   | `ncpKeyId` + 서버 Client Secret |
| 11  | TMAP 보행자 경로안내                           | 보행 경로 GeoJSON               | 요청 시                   | 서버 appKey                     |
| 12  | Gemini API                                     | 약봉투 판독                     | 요청 시                   | API 키                          |
| 13  | EAS (Base Sepolia 테스트넷)                    | 온체인 증명                     | 이벤트 시                 | 지갑 키                         |

> **최적화 O-1.** 위험도 계산은 쉼터가 아니라 **대상자 위치**의 기상을 사용한다. 현재 가상 대상자 5명은 API허브 500m 지점 5개와 단기예보 5km 격자 3개로 중복 제거한다. 30분 주기 기준 1차 관측 5회와 전역 특보 1회를 합쳐 하루 288회이며, 각 호출을 한 번 재시도해도 576회(현재 API허브 화면의 일 20,000회 한도 대비 2.88%)다. 계산 근거와 전송량 게이트는 `docs/weather-call-budget.md`를 따른다.
>
> **최적화 O-2.** 모든 외부 응답은 `stale-while-revalidate`로 캐싱한다. 기상 데이터 TTL 25분, 의약품 데이터 TTL 30일, 건물 높이 영구 캐시.

---

## 6. 디자인 시스템

### 6.1 디자인 컨셉

**개념 — 등온(等溫).** 이 제품에서 **색은 장식이 아니라 데이터다.** 모든 색상 결정은 온도 스케일 위의 위치를 의미한다. 사용자가 색을 보는 순간 수치를 읽지 않아도 위험 수준을 안다.

**표면 이원화.** 하나의 토큰 체계, 두 개의 표면.

| 표면   | 이름             | 사용자       | 근거                                                    |
| ------ | ---------------- | ------------ | ------------------------------------------------------- |
| 다크   | **Shade (그늘)** | U-A 관제     | 상황실 환경. 어두운 배경에서 열 색상 대비가 극대화된다. |
| 라이트 | **Paper (백사)** | U-B/C/D 시민 | 야외 직사광 아래 스마트폰 가독성. 저시력 고령자 대비.   |

**시그니처 — 등온 리본(Isotherm Ribbon).** 5단계 열 스케일을 가로 막대로 표현하고 현재 위치에 마커를 놓는 단일 장치. 이 장치 하나가 헤더 상태 표시, 리스트 행 인디케이터, 지도 범례, 시계열 타임라인 네 곳에 형태만 바꿔 재사용된다. 다른 장식은 최소화한다.

### 6.2 색상 팔레트

#### 6.2.1 등온 스케일 (의미색 — 최우선)

| 토큰       | HEX       | 등급    | 용도                        |
| ---------- | --------- | ------- | --------------------------- |
| `--heat-0` | `#0B6E6B` | L0 안전 | 안전 상태, 그늘 구간        |
| `--heat-1` | `#A67C00` | L1 관심 | 관심 배지                   |
| `--heat-2` | `#D2601A` | L2 주의 | 주의 배지, 주의 대상 강조   |
| `--heat-3` | `#C0261B` | L3 경고 | 경고 배지, 알림 발송        |
| `--heat-4` | `#E01B24` | L4 위험 | 최고 경보 + 펄스 애니메이션 |

**대비 검증 (배경 대비율)**

| 토큰       | on Shade `#0D1418` | on Paper `#F1F5F6` | 판정                                      |
| ---------- | ------------------ | ------------------ | ----------------------------------------- |
| `--heat-0` | 4.9:1              | 4.6:1              | AA (대형 텍스트·아이콘)                   |
| `--heat-2` | 6.1:1              | 3.8:1              | Paper에서는 텍스트 사용 금지, 배경/보더만 |
| `--heat-4` | 5.4:1              | 4.7:1              | AA                                        |

> **규칙 C-1.** `--heat-*`를 Paper 표면의 **본문 텍스트 색으로 사용하지 않는다.** 배지 배경, 보더, 아이콘에만 사용하고 텍스트는 `--paper-fg`를 쓴다.

#### 6.2.2 표면색

**Shade (다크)**

| 토큰              | HEX       | 용도                 |
| ----------------- | --------- | -------------------- |
| `--shade-bg`      | `#0D1418` | 페이지 배경          |
| `--shade-raised`  | `#151F24` | 카드·패널            |
| `--shade-overlay` | `#1D2A30` | 모달·드롭다운        |
| `--shade-border`  | `#26363D` | 구분선 (1px)         |
| `--shade-fg`      | `#E8F0F2` | 본문 텍스트 (14.8:1) |
| `--shade-fg-2`    | `#9DB2B9` | 보조 텍스트 (7.2:1)  |
| `--shade-fg-3`    | `#6B838C` | 비활성 (4.6:1)       |

**Paper (라이트)**

| 토큰             | HEX       | 용도           |
| ---------------- | --------- | -------------- |
| `--paper-bg`     | `#F1F5F6` | 페이지 배경    |
| `--paper-raised` | `#FFFFFF` | 카드           |
| `--paper-border` | `#D3DEE1` | 구분선         |
| `--paper-fg`     | `#0D1418` | 본문 (16.9:1)  |
| `--paper-fg-2`   | `#4A5C63` | 보조 (7.8:1)   |
| `--paper-fg-3`   | `#7A8C93` | 비활성 (4.5:1) |

#### 6.2.3 기능색

| 토큰             | HEX       | 용도                              |
| ---------------- | --------- | --------------------------------- |
| `--brand`        | `#0B6E6B` | 주요 액션 버튼, 링크              |
| `--brand-hover`  | `#095A57` | hover                             |
| `--brand-active` | `#074845` | active                            |
| `--attest`       | `#4338CA` | 온체인 검증 전용 (다른 용도 금지) |
| `--attest-bg`    | `#EEF0FE` | 검증 배지 배경 (Paper)            |
| `--im-bank`      | `#1B4B9E` | iM뱅크 쉼터 마커 전용             |
| `--danger`       | `#E01B24` | 파괴적 액션                       |
| `--focus`        | `#4DD4C8` | 포커스 링 (양 표면 공통)          |

> **규칙 C-2.** `--brand`가 `--heat-0`와 동일 계열인 것은 의도적이다. **그늘 = 안전 = 제품의 목적**이라는 의미 연결이다. 임의로 분리하지 않는다.
>
> **규칙 C-3.** 색상만으로 정보를 전달하지 않는다. 모든 등급 표시는 `색 + 텍스트 라벨 + 아이콘 형태` 3중으로 제공한다 (§13.3).

### 6.3 타이포그래피

#### 6.3.1 폰트 패밀리

| 역할        | 폰트                     | 라이선스 | 웨이트             |
| ----------- | ------------------------ | -------- | ------------------ |
| Display     | **Wanted Sans Variable** | SIL OFL  | 600, 700, 800      |
| Body        | **Pretendard Variable**  | SIL OFL  | 400, 500, 600, 700 |
| Data / Mono | **JetBrains Mono**       | SIL OFL  | 400, 700           |

```css
--font-display: "Wanted Sans Variable", "Pretendard Variable", -apple-system, sans-serif;
--font-body: "Pretendard Variable", -apple-system, "Malgun Gothic", sans-serif;
--font-mono: "JetBrains Mono", "D2Coding", ui-monospace, monospace;
```

로딩: `<link rel="preload" as="font">` + `font-display: swap`. Display는 서브셋(한글 2,350자 + 라틴)으로 분리한다.

#### 6.3.2 타입 스케일

| 토큰      | size/line | weight | letter-spacing | 폰트    | 용도                 |
| --------- | --------- | ------ | -------------- | ------- | -------------------- |
| `d1`      | 40/48     | 800    | -0.02em        | Display | 페이지 최상단 타이틀 |
| `d2`      | 32/40     | 700    | -0.02em        | Display | 섹션 헤드            |
| `h1`      | 28/36     | 700    | -0.01em        | Display | 화면 제목            |
| `h2`      | 24/32     | 600    | -0.01em        | Body    | 카드 제목            |
| `h3`      | 20/28     | 600    | 0              | Body    | 서브 제목            |
| `body-l`  | 18/28     | 400    | 0              | Body    | **Paper 기본 본문**  |
| `body`    | 16/26     | 400    | 0              | Body    | **Shade 기본 본문**  |
| `body-s`  | 14/22     | 400    | 0              | Body    | 보조 설명            |
| `caption` | 13/18     | 500    | 0.01em         | Body    | 라벨·캡션            |
| `data-xl` | 56/56     | 700    | -0.03em        | Mono    | HRI 점수, 체감온도   |
| `data-l`  | 32/36     | 700    | -0.02em        | Mono    | 카드 내 수치         |
| `data-s`  | 14/20     | 400    | 0              | Mono    | 해시, 좌표, UID      |

#### 6.3.3 타이포그래피 규칙

- 모든 수치 표시에 `font-variant-numeric: tabular-nums`를 적용한다.
- 제목(`d1`~`h2`)에 `text-wrap: balance`를 적용한다.
- 말줄임은 `…`(U+2026)을 사용한다. `...` 금지.
- 로딩 문구는 `…`로 끝낸다. 예: `위험도 계산 중…`
- 단위와 수치 사이는 비분리 공백: `39.2&nbsp;℃`, `300&nbsp;m`
- 브랜드명·코드·해시는 `translate="no"` 속성을 부여한다.

#### 6.3.4 시니어 모드

`subjects.senior_mode = true` 또는 사용자 토글 시 Paper 표면에만 적용한다.

| 항목         | 기본 | 시니어 모드      |
| ------------ | ---- | ---------------- |
| 본문 크기    | 18px | **22px** (×1.25) |
| 최소 탭 영역 | 48px | **56px**         |
| 버튼 높이    | 48px | **60px**         |
| 행간 배수    | 1.55 | **1.7**          |
| 자간         | 0    | **0.01em**       |
| 아이콘 크기  | 20px | **28px**         |

토글은 `localStorage` + 서버 프로필 양쪽에 저장하고, 서버 값을 우선한다.

---

## 7. 레이아웃 규칙

### 7.1 간격 스케일 (4px 기반)

```
--sp-1: 4px    --sp-2: 8px    --sp-3: 12px   --sp-4: 16px
--sp-5: 20px   --sp-6: 24px   --sp-8: 32px   --sp-10: 40px
--sp-12: 48px  --sp-16: 64px  --sp-20: 80px
```

**적용 규칙**

| 관계             | 간격                                      |
| ---------------- | ----------------------------------------- |
| 아이콘 ↔ 라벨    | `--sp-2` (8px)                            |
| 폼 라벨 ↔ 입력창 | `--sp-2` (8px)                            |
| 폼 필드 간       | `--sp-5` (20px)                           |
| 카드 내부 패딩   | `--sp-6` (24px) / 모바일 `--sp-4` (16px)  |
| 카드 간          | `--sp-4` (16px)                           |
| 섹션 간          | `--sp-12` (48px) / 모바일 `--sp-8` (32px) |
| 페이지 상하 여백 | `--sp-16` (64px) / 모바일 `--sp-8` (32px) |

### 7.2 모서리 반경

```
--r-0: 0      --r-1: 4px    --r-2: 8px
--r-3: 12px   --r-4: 16px   --r-full: 9999px
```

| 요소          | 반경           |
| ------------- | -------------- |
| 버튼          | `--r-2` (8px)  |
| 입력창        | `--r-2` (8px)  |
| 카드          | `--r-3` (12px) |
| 모달·바텀시트 | `--r-4` (16px) |
| 배지·칩       | `--r-full`     |
| 등온 리본     | `--r-1` (4px)  |

### 7.3 그림자 (Paper 표면 전용)

```css
--sh-1: 0 1px 2px rgba(13, 20, 24, 0.06), 0 1px 3px rgba(13, 20, 24, 0.04);
--sh-2: 0 4px 8px rgba(13, 20, 24, 0.06), 0 2px 4px rgba(13, 20, 24, 0.04);
--sh-3: 0 12px 24px rgba(13, 20, 24, 0.1), 0 4px 8px rgba(13, 20, 24, 0.06);
```

> **규칙 L-1.** Shade 표면에서는 그림자 대신 `--shade-border` 1px 보더와 배경 명도차로 계층을 표현한다. 다크 배경의 그림자는 보이지 않는다.

### 7.4 그리드와 컨테이너

| 표면           | 컨테이너 최대폭    | 컬럼 | 거터 |
| -------------- | ------------------ | ---- | ---- |
| Shade 대시보드 | `1600px`           | 12   | 24px |
| Paper 모바일   | `100%` (좌우 16px) | 4    | 16px |
| Paper 태블릿   | `768px`            | 8    | 20px |
| Paper 데스크톱 | `1024px`           | 12   | 24px |

**Shade 대시보드 골격**

```
┌────────────────────────────────────────────────────┐
│ TopBar  h=64px  (fixed)                            │
├──────────┬─────────────────────────────────────────┤
│ SideNav  │ Content                                 │
│ w=240px  │ padding: 32px                           │
│ (fixed)  │ max-width: 1600px                       │
│          │                                         │
│ ≤1280px  │                                         │
│ → 72px   │                                         │
│ 아이콘만 │                                         │
└──────────┴─────────────────────────────────────────┘
```

### 7.5 z-index 체계

```
--z-base: 0        --z-sticky: 100    --z-topbar: 200
--z-dropdown: 300  --z-overlay: 400   --z-modal: 500
--z-toast: 600     --z-alert-l4: 700
```

> **규칙 L-2.** L4 위험 경보는 모든 레이어 위에 표시된다. 모달이 열려 있어도 가려지지 않는다.

---

## 8. 공통 컴포넌트 규격

### 8.1 Button

**변형(variant)**

| variant     | 배경        | 텍스트     | 보더          | 용도                 |
| ----------- | ----------- | ---------- | ------------- | -------------------- |
| `primary`   | `--brand`   | `#FFFFFF`  | none          | 화면당 1개 주요 액션 |
| `secondary` | transparent | `--brand`  | 1px `--brand` | 보조 액션            |
| `ghost`     | transparent | `--*-fg-2` | none          | 3순위 액션           |
| `danger`    | `--danger`  | `#FFFFFF`  | none          | 파괴적 액션          |
| `attest`    | `--attest`  | `#FFFFFF`  | none          | 온체인 검증 전용     |

**크기(size)**

| size     | 높이 | 좌우 패딩 | 폰트     | 아이콘 | 사용 표면       |
| -------- | ---- | --------- | -------- | ------ | --------------- |
| `sm`     | 32px | 12px      | `body-s` | 16px   | Shade 밀집 영역 |
| `md`     | 40px | 16px      | `body`   | 20px   | Shade 기본      |
| `lg`     | 48px | 20px      | `body-l` | 20px   | **Paper 기본**  |
| `xl`     | 56px | 24px      | `body-l` | 24px   | Paper 주요 CTA  |
| `senior` | 60px | 28px      | 22px     | 28px   | 시니어 모드     |

**상태**

| 상태            | 명세                                                                                      |
| --------------- | ----------------------------------------------------------------------------------------- |
| `hover`         | 배경 명도 −8%, `transition: background-color 120ms ease-out, border-color 120ms ease-out` |
| `active`        | 배경 명도 −16%, `transform: scale(0.98)`                                                  |
| `focus-visible` | `outline: 2px solid var(--focus); outline-offset: 2px`                                    |
| `disabled`      | `opacity: .45; cursor: not-allowed; pointer-events: none`                                 |
| `loading`       | 라벨 유지 + 좌측 16px 스피너, `aria-busy="true"`, 클릭 차단                               |

**필수 규칙**

- 아이콘 전용 버튼은 `aria-label` 필수.
- 장식용 아이콘은 `aria-hidden="true"`.
- `transition: all` 금지. 속성을 명시한다.
- `outline: none`을 focus 대체 없이 사용 금지.
- 제출 버튼은 요청 시작 전까지 활성 상태를 유지한다.
- 라벨은 구체적으로. `계속` 금지 → `쉼터 경로 받기`, `복약 정보 저장`.

```tsx
<Button variant="primary" size="lg" loading={isPending}>
  {isPending ? "경로 계산 중…" : "그늘 경로 받기"}
</Button>
```

### 8.2 Card

**기본 규격**

| 속성                 | Shade                | Paper                     |
| -------------------- | -------------------- | ------------------------- |
| 배경                 | `--shade-raised`     | `--paper-raised`          |
| 보더                 | 1px `--shade-border` | 1px `--paper-border`      |
| 반경                 | 12px                 | 12px                      |
| 패딩                 | 24px (모바일 16px)   | 24px (모바일 16px)        |
| 그림자               | 없음                 | `--sh-1`                  |
| hover (클릭 가능 시) | 보더 `--brand`       | `--sh-2` + 보더 `--brand` |

**파생 카드**

`RiskCard` — 대상자 위험도 카드

```
┌──────────────────────────────────────────┐
│ ▌ [L3 경고]         김○○ · 82세 · 독거   │  ← ▌= 좌측 4px 등온 바
│                                          │
│    72          체감 39.2℃ · 수성구 파동   │  ← data-xl
│                                          │
│  · 이뇨제·항콜린제 복용 (+12)             │  ← reasons 최대 3
│  · 체감 39.2℃ + 폭염경보 (+47)            │
│                                          │
│  [ 쉼터 경로 발송 ]                       │
└──────────────────────────────────────────┘
```

- 좌측 4px 세로 바는 `--heat-{level}` 색.
- L4일 때만 카드 보더에 펄스 애니메이션 적용 (`prefers-reduced-motion` 시 정적 보더로 대체).

`ShelterCard` — 쉼터 카드

```
┌──────────────────────────────────────────┐
│ DGB대구은행 서문시장지점        [iM뱅크] │
│ 금융기관 · 중구 · 320m · 도보 7분        │
│                                          │
│ ● 운영 중   14분 전 확인   [✓ 검증됨]    │
│ [Base Sepolia 테스트넷]                   │
│ 그늘 비율 68%                            │
└──────────────────────────────────────────┘
```

### 8.3 Input

**텍스트 입력**

| 속성     | 값                                                                      |
| -------- | ----------------------------------------------------------------------- |
| 높이     | Shade 40px / Paper 48px / 시니어 60px                                   |
| 패딩     | 좌우 12px                                                               |
| 보더     | 1px `--*-border`                                                        |
| 반경     | 8px                                                                     |
| 폰트     | Shade `body` / Paper `body-l`                                           |
| focus    | 보더 `--brand` + `outline: 2px solid var(--focus); outline-offset: 1px` |
| error    | 보더 `--danger` + 하단 12px 위치에 `caption` 오류 메시지                |
| disabled | 배경 명도 −4%, `opacity: .6`                                            |

**필수 규칙**

- 모든 입력은 `<label htmlFor>` 또는 `aria-label`을 가진다.
- `type`과 `inputmode`를 정확히 지정한다: 전화 `type="tel" inputmode="numeric"`, 나이 `type="number" inputmode="numeric"`.
- `autocomplete` 지정: 전화 `tel`, 이름 `name`, 주소 `street-address`.
- **붙여넣기 차단 금지** (`onPaste` + `preventDefault` 사용 불가).
- 코드·전화·주소 입력은 `spellCheck={false}`.
- 플레이스홀더는 `…`로 끝내고 예시 패턴을 보인다: `예: 010-1234-5678…`
- 제출 시 첫 번째 오류 필드로 포커스를 이동한다.
- 오류는 필드 바로 아래 인라인 표시한다. 상단 요약 배너 단독 사용 금지.

**카메라 입력 (F-02 전용)**

```tsx
<input type="file" accept="image/*" capture="environment" aria-label="약봉투 사진 촬영" />
```

- 최대 파일 크기 10MB. 초과 시 클라이언트에서 리사이즈(장변 2048px).
- 촬영 가이드 오버레이: 약봉투 외곽선 프레임 + `약봉투 글자가 프레임 안에 들어오게 찍어주세요` 안내.

### 8.4 Modal / BottomSheet

| 표면             | 형태      | 규격                                             |
| ---------------- | --------- | ------------------------------------------------ |
| Shade (데스크톱) | 중앙 모달 | 최대폭 560px, 반경 16px, 패딩 32px               |
| Paper (모바일)   | 바텀시트  | 전폭, 상단 반경 16px, 패딩 24px, 최대 높이 90dvh |

**필수 규칙**

- 오버레이 `rgba(13,20,24,.62)`, `--z-overlay`.
- 열릴 때 포커스를 모달 내부 첫 요소로 이동, 닫힐 때 트리거로 복귀.
- 포커스 트랩 필수. `Esc`로 닫기 가능.
- `overscroll-behavior: contain` 적용 (배경 스크롤 방지).
- 바텀시트는 `env(safe-area-inset-bottom)` 만큼 하단 패딩 추가.
- **파괴적 액션은 반드시 확인 모달 또는 실행 취소 창을 거친다.** 즉시 실행 금지.
- 모바일에서 `autoFocus` 사용 금지 (키보드 강제 팝업 방지).

### 8.5 Badge / Chip

**등급 배지 (RiskBadge)**

| 등급    | 배경           | 텍스트     | 아이콘 형태   |
| ------- | -------------- | ---------- | ------------- |
| L0 안전 | `--heat-0` 12% | `--heat-0` | ● 원형        |
| L1 관심 | `--heat-1` 12% | `--heat-1` | ◆ 마름모      |
| L2 주의 | `--heat-2` 14% | `--heat-2` | ▲ 삼각형      |
| L3 경고 | `--heat-3` 14% | `--heat-3` | ■ 사각형      |
| L4 위험 | `--heat-4`     | `#FFFFFF`  | ✕ 십자 + 펄스 |

규격: 높이 24px, 좌우 패딩 10px, `caption` 폰트, `--r-full`.

> **규칙 B-1.** 아이콘 형태가 등급마다 다른 것은 색맹 사용자 대응이다. 형태를 통일하지 않는다.

**검증 배지 (AttestBadge)**

| 상태         | 표시         | 색                         |
| ------------ | ------------ | -------------------------- |
| `VERIFIED`   | `✓ 검증됨`   | `--attest`                 |
| `PENDING`    | `⋯ 기록 중…` | `--*-fg-3`                 |
| `UNVERIFIED` | `미검증`     | `--*-fg-3` + 1px 점선 보더 |
| `FAILED`     | `기록 실패`  | `--danger`                 |

클릭 시 S-07 검증 페이지로 이동한다 (`<a>` 요소 사용, 새 탭). 배지 단독으로 네트워크를 숨기지 않고 인접한 `Base Sepolia 테스트넷` NetworkChip을 항상 렌더링하며, 보조기술용 이름에도 같은 네트워크명을 포함한다.

### 8.6 등온 리본 (Isotherm Ribbon) — 시그니처

5구간 가로 막대 + 현재 위치 마커.

```
안전    관심    주의    경고    위험
├──────┼──────┼──────┼──────┼──────┤
                        ▲
                       72
```

**규격**

| 형태     | 높이                 | 사용처               |
| -------- | -------------------- | -------------------- |
| `full`   | 12px, 폭 100%        | S-01 상단, S-02 헤더 |
| `inline` | 6px, 폭 120px        | 리스트 행            |
| `bar`    | 4px, 세로, 높이 100% | 카드 좌측 인디케이터 |
| `legend` | 8px, 폭 200px        | 지도 범례            |

- 각 구간 폭은 등급 범위 비율에 비례한다 (20/20/20/20/20).
- 마커는 `transform: translateX()`로만 이동한다. `left` 애니메이션 금지.
- `transition: transform 240ms cubic-bezier(.2,.8,.2,1)`.
- `prefers-reduced-motion` 시 트랜지션 제거.

### 8.7 Toast

| 속성     | 값                                                   |
| -------- | ---------------------------------------------------- |
| 위치     | Shade 우하단 24px / Paper 상단 16px (safe-area 고려) |
| 폭       | 최대 400px (모바일 전폭 −32px)                       |
| 지속     | 성공/정보 4초, **오류는 수동 닫기까지 유지**         |
| 컨테이너 | `aria-live="polite"`, 오류는 `aria-live="assertive"` |
| 스택     | 최대 3개, 초과 시 오래된 것부터 제거                 |

오류 토스트는 반드시 **다음 행동**을 포함한다. `저장 실패` 금지 → `저장 실패 — 네트워크를 확인하고 다시 시도하세요. [다시 시도]`

### 8.8 Table (대상자 목록)

- `font-variant-numeric: tabular-nums` 필수.
- 행 50개 초과 시 `virtua` 가상 스크롤 적용.
- 헤더 `position: sticky; top: 64px` (TopBar 높이만큼).
- 정렬 상태는 URL 쿼리에 반영: `?sort=hri&order=desc`.
- 행 클릭은 `<a>`로 감싼다 (Cmd+클릭·중간클릭 지원).
- 행 높이 56px, 시니어 모드 미적용(관제 전용).

### 8.9 Map

| 속성            | 값                            |
| --------------- | ----------------------------- |
| 라이브러리      | Naver Maps JS API v3          |
| 기본 중심       | 대구 중구 `35.8714, 128.6014` |
| 기본 줌         | level 6                       |
| 마커 클러스터링 | 50개 초과 시 활성             |

**마커 규격**

| 종류               | 색                      | 크기 | 형태            |
| ------------------ | ----------------------- | ---- | --------------- |
| 일반 쉼터 (운영중) | `--heat-0`              | 28px | 원형            |
| 일반 쉼터 (미확인) | `--*-fg-3`              | 28px | 원형 점선       |
| **iM뱅크 쉼터**    | `--im-bank`             | 32px | 사각형 + 라운드 |
| 대상자 (L3↑)       | `--heat-3` / `--heat-4` | 36px | 핀 + 펄스       |
| 현재 위치          | `--brand`               | 20px | 원형 + 외곽 링  |

**경로 폴리라인**

| 구간      | 색         | 굵기 | 패턴 |
| --------- | ---------- | ---- | ---- |
| 그늘 구간 | `--heat-0` | 6px  | 실선 |
| 햇빛 구간 | `--heat-2` | 6px  | 실선 |
| 대체 경로 | `--*-fg-3` | 4px  | 점선 |

지도는 키보드만으로 조작 불가하므로, **모든 지도 정보는 하단 리스트로 동등하게 제공한다** (§13.5).

---

## 9. 화면별 상세 명세

### 화면 목록

| ID   | 화면             | 표면  | 라우트                    | 사용자   |
| ---- | ---------------- | ----- | ------------------------- | -------- |
| S-01 | 관제 대시보드    | Shade | `/dashboard`              | U-A      |
| S-02 | 대상자 상세      | Shade | `/subjects/[id]`          | U-A      |
| S-03 | 복약 등록·판독   | 공용  | `/medication/[subjectId]` | U-A, U-B |
| S-05 | 쉼터 지도·그늘길 | Paper | `/shelters`               | U-B, U-C |
| S-06 | 쉼터 상태 제보   | Paper | `/report/[shelterId]`     | U-B, U-C |
| S-07 | 온체인 증명 검증 | Paper | `/verify/[uid]`           | U-D      |
| S-08 | 보호자 알림 상세 | Paper | `/alert/[eventId]`        | U-B      |

---

### S-01 · 관제 대시보드

**목적.** 담당자가 화면을 여는 순간 **오늘 누가 위험한지** 3초 안에 파악한다.

**와이어프레임 (1440px)**

```
┌────────────────────────────────────────────────────────────────┐
│ 온중  │ 대구 수성구        체감 39.2℃  폭염경보    [KO] [설정] │ 64px
├───────┼────────────────────────────────────────────────────────┤
│ ▣대시 │ ══════════ 등온 리본 (full) ══════════════════════▲   │
│ ▤명단 │                                                        │
│ ▦쉼터 │ ┌─────────┬─────────┬─────────┐                       │
│ ⛓증명 │ │ 위험 L4 │ 경고 L3 │ 주의 L2 │  ← 요약 3칸          │
│       │ │    3    │   12    │   38    │                       │
│       │ └─────────┴─────────┴─────────┘                       │
│       │                                                        │
│       │ 즉시 조치 필요                        [전체 명단 →]    │
│       │ ┌────────────────────────────────────────────────┐    │
│       │ │ ▌[L4] 박○○ · 87세 · 독거      92        [경로] │    │
│       │ ├────────────────────────────────────────────────┤    │
│       │ │ ▌[L4] 김○○ · 82세 · 독거      88        [경로] │    │
│       │ └────────────────────────────────────────────────┘    │
│       │                                                        │
│       │ ┌──────────────────────┬───────────────────────────┐  │
│       │ │  위험도 지도          │  최근 온체인 기록         │  │
│       │ │                       │  Base Sepolia 테스트넷    │  │
│       │ │  (마커 + 클러스터)    │  ✓ 14:03 보호자 알림 L3  │  │
│       │ │                       │  ✓ 13:47 체크인 서문시장  │  │
│       │ └──────────────────────┴───────────────────────────┘  │
└───────┴────────────────────────────────────────────────────────┘
```

**구성 요소**

| 영역        | 컴포넌트                                | 규격                                                    |
| ----------- | --------------------------------------- | ------------------------------------------------------- |
| TopBar      | 지역 셀렉터 + 현재 체감온도 + 특보 배지 | h=64px, `--z-topbar`                                    |
| SideNav     | 4개 항목                                | w=240px, ≤1280px에서 72px 아이콘 전용                   |
| 등온 리본   | `full` 변형                             | h=12px, 마커=관할 평균 HRI                              |
| 요약 카드   | 3칸 그리드                              | 각 `data-l` 수치 + `caption` 라벨                       |
| 즉시 조치   | RiskCard 리스트                         | L3 이상만, HRI 내림차순, 최대 10건                      |
| 지도        | Map 컴포넌트                            | 7 컬럼                                                  |
| 온체인 피드 | 리스트                                  | 5 컬럼, 최근 10건 + `Base Sepolia 테스트넷` NetworkChip |

**동작 규칙**

- 데이터는 30초마다 자동 갱신한다. 갱신 중 기존 데이터를 유지하고 우상단에 `갱신 중…` 표시.
- L4 대상자가 신규 발생하면 `--z-alert-l4` 레이어에 상단 알림 바를 표시한다. 사용자가 닫기 전까지 유지.
- 필터 상태는 URL에 반영한다: `?gu=수성구&level=L3`

**빈 상태.** `현재 L3 이상 대상자가 없습니다.` + 부제 `체감온도 31℃ 미만이면 위험도가 자동으로 낮아집니다.`

---

### S-02 · 대상자 상세

**와이어프레임**

```
┌────────────────────────────────────────────────────────────┐
│ ← 명단으로                                                  │
│                                                            │
│ 김○○  82세 · 여 · 독거                     [L3 경고]      │
│ 수성구 파동로3길 62                                        │
│ ══════════ 등온 리본 (full) ═════════▲ 70 ══════           │
│                                                            │
│ ┌─ 위험도 구성 ──────────────────────────────────────────┐│
│ │ 환경 E        47 ████████                               ││
│ │ 복약 M        12 ███                                    ││
│ │ 개인 P        17 ████                                   ││
│ │ 완화 C        −6                                        ││
│ │ ─────────────────                                        ││
│ │ 합계          70                                        ││
│ └────────────────────────────────────────────────────────┘│
│                                                            │
│ 복약 정보  [3건]                          [+ 약봉투 촬영]  │
│ ┌────────────────────────────────────────────────────────┐│
│ │ 라식스정 40mg      이뇨제        [고위험 +6] [자동인식] ││
│ │ 아리셉트정 5mg     항치매제      [중위험 +3] [자동인식] ││
│ │ 지르텍정 10mg      항히스타민제  [고위험 +6] [확인필요] ││
│ └────────────────────────────────────────────────────────┘│
│                                                            │
│ 돌봄 이력 (온체인)  [Base Sepolia 테스트넷]                │
│ ┌────────────────────────────────────────────────────────┐│
│ │ ✓ 08-23 14:03  보호자 알림 · L3  0x7a3f…d2  [검증]    ││
│ │ ✓ 08-22 15:20  체크인 · 서문시장  0x91c…4e  [검증]     ││
│ └────────────────────────────────────────────────────────┘│
│                                                            │
│ [ 쉼터 경로 발송 ]  [ 보호자 알림 ]                         │
└────────────────────────────────────────────────────────────┘
```

**규칙**

- 위험도 구성 막대는 각 항목 최대치 대비 비율로 표현한다 (E는 /50, M은 /25, P는 /20, C는 /6).
- 복약 카드의 `확인 필요` 배지는 클릭 시 인라인 편집 모드로 전환된다.
- 하단 액션 버튼은 모바일에서 `position: sticky; bottom: 0` 처리한다.
- 개인정보 표시는 이름 마스킹(`김○○`)을 기본으로 하고, 담당자 권한에서만 전체 표시 토글을 제공한다.

---

### S-03 · 복약 등록·판독

**3단계 흐름**

```
[1 촬영]  →  [2 판독 결과 확인]  →  [3 저장 완료]
```

**단계 1 — 촬영**

```
┌──────────────────────────┐
│ 약봉투 촬영               │
│                          │
│ ┌──────────────────────┐ │
│ │  ┌────────────────┐  │ │  ← 카메라 프리뷰
│ │  │                │  │ │     + 가이드 프레임
│ │  │   가이드 프레임 │  │ │
│ │  │                │  │ │
│ │  └────────────────┘  │ │
│ └──────────────────────┘ │
│                          │
│ 약봉투 글자가 프레임 안에 │
│ 들어오게 찍어주세요       │
│                          │
│ [    촬영하기    ]  xl    │
│ [ 앨범에서 선택 ]  ghost  │
│ [ 직접 입력하기 ]  ghost  │
└──────────────────────────┘
```

**단계 2 — 판독 결과**

- 인식된 약품을 카드 리스트로 표시한다.
- `confidence ≥ 0.85` → 배지 `자동 인식`, 체크박스 기본 선택.
- `0.60 ≤ confidence < 0.85` → 배지 `확인 필요`, 노란 보더, 수정 입력창 노출, 체크박스 기본 선택.
- `confidence < 0.60` → 목록에서 제외하고 하단에 `인식하지 못한 항목 N건 — 직접 입력` 링크 제공.
- 각 약품 옆에 위험 계열과 점수를 표시한다: `이뇨제 · 고위험 +6`.
- 하단 요약: `복약으로 인한 추가 위험도 +12점`

**단계 3 — 저장 완료**

- 변경 전후 HRI를 나란히 표시한다: `60 → 72 (+12)`
- 고정 면책 문구를 표시한다 (§3.2 규칙 F2-1).

**로딩 상태.** 판독은 3–8초 소요된다. 스켈레톤이 아니라 **단계 표시**를 쓴다.

```
● 사진 분석 중…
○ 의약품 조회 중…
○ 위험도 계산 중…
```

---

### S-05 · 쉼터 지도·그늘길 (모바일 우선)

**와이어프레임 (390px)**

```
┌──────────────────────────┐
│ 지금 가장 시원한 곳       │  h1
│ 수성구 · 체감 39.2℃      │
├──────────────────────────┤
│ [전체][iM뱅크][운영중]    │  ← 필터 칩
├──────────────────────────┤
│                          │
│      지도 (h=280px)       │
│    ● ● ■(iM) ●           │
│         ▲내위치           │
│                          │
├──────────────────────────┤
│ ┌──────────────────────┐ │
│ │DGB대구은행 서문시장지점│ │
│ │        [iM뱅크]       │ │
│ │금융기관 · 320m · 7분  │ │
│ │● 운영 중  14분 전 확인│ │
│ │[✓검증됨] [Base Sepolia 테스트넷]│ │
│ │                      │ │
│ │[   그늘 경로 받기   ] │ │
│ └──────────────────────┘ │
│ ┌──────────────────────┐ │
│ │파동행정복지센터       │ │
│ │ …                    │ │
│ └──────────────────────┘ │
└──────────────────────────┘
```

**정렬 규칙 (기본)**

```
1순위: 운영 확인됨(온체인 검증) & 거리 500m 이내
2순위: 그늘 비율 내림차순
3순위: 거리 오름차순
```

**경로 상세 (바텀시트)**

```
┌──────────────────────────┐
│ ━━                       │  ← 드래그 핸들
│ 그늘 경로 · 도보 7분      │
│ 320m · 그늘 68%          │
│                          │
│ ▓▓▓▓▓▓░░▓▓▓▓▓▓▓▓░░▓▓    │  ← 구간별 그늘 바
│ 그늘         햇빛        │
│                          │
│ ⚠ 햇빛 구간 2곳 (합 102m)│
│ 어르신 걸음 기준 7분입니다│
│                          │
│ [  네이버 지도로 열기  ]  │
│ [  도착 알림 받기    ]   │
│ [ 도착 체크인 (인증 필요)] │
└──────────────────────────┘
```

- `도착 체크인`은 인증 담당자 또는 해당 대상자 범위의 유효한 접근 토큰에만 표시·허용한다. 공개 사용자가 같은 요청을 보내면 `shelter_reports`로 우회하지 않고 `403`을 반환한다.
- 체크인 저장 직후에는 `기록 확인 중 · Base Sepolia 테스트넷`을 표시한다. `VERIFIED` 전에는 HRI C를 차감하지 않는다.

---

### S-06 · 쉼터 상태 제보

**와이어프레임**

```
┌──────────────────────────┐
│ ← 지도로                  │
│                          │
│ DGB대구은행 서문시장지점  │  h2
│ 중구 대신동 115-378       │
│                          │
│ 지금 문을 열었나요?       │  h3
│ ┌──────────┬───────────┐ │
│ │  열림     │   닫힘    │ │  ← 각 h=72px
│ │    ●     │     ○     │ │
│ └──────────┴───────────┘ │
│                          │
│ 얼마나 붐비나요? (선택)   │
│ [여유] [보통] [혼잡]      │
│                          │
│ ┌──────────────────────┐ │
│ │ ⛓ 이 제보는 블록체인에│ │
│ │ 발급·폐기 이력을 공개  │ │
│ │ 검증할 수 있습니다     │ │
│ │ Base Sepolia 테스트넷  │ │
│ └──────────────────────┘ │
│                          │
│ [   제보하고 기록하기   ] │  xl, attest
└──────────────────────────┘
```

**규칙**

- 열림/닫힘 선택은 라디오 그룹이며 라벨 전체가 탭 영역이다 (사각지대 없음).
- 제보 후 즉시 낙관적 UI 갱신 → 온체인 기록 완료 시 `PENDING → VERIFIED`.
- 동일 사용자가 동일 쉼터에 10분 내 재제보하는 것을 차단한다. 차단 시 안내: `방금 제보하셨습니다. 10분 후에 다시 제보할 수 있습니다.`
- S-06은 로그인 없이 제출하는 **공개 운영상태 제보**이며 `shelter_reports`에만 저장한다. 대상자 선택·대상자 토큰·체크인 UI를 포함하지 않고, 성공해도 `shelter_checkins`를 생성하거나 C−6을 적용하지 않는다.
- EAS 기록은 오프체인 제보 저장과 분리한다. DB 저장 성공 후 `ShelterStatus` 작업을 만들며, 테스트넷 장애가 나도 제보 완료를 롤백하지 않는다.

---

### S-07 · 온체인 증명 검증 (공개)

**목적.** 심사위원·감사자가 링크만으로 기록의 진위를 확인한다. **로그인 불필요.**

```
┌────────────────────────────────────────────┐
│                    ⛓                        │
│              검증 완료                      │
│          Base Sepolia 테스트넷              │
│                                            │
│ 이 기록은 2026-08-23 14:03:12에 생성된 후   │
│ 수정되지 않았습니다.                        │
│                                            │
│ ┌────────────────────────────────────────┐ │
│ │ 유형        보호자 알림 발송             │ │
│ │ 위험 등급   L3 경고                     │ │
│ │ HRI         72                          │ │
│ │ 발생 시각   2026-08-23 14:03:12 KST     │ │
│ │ 대상자 해시 0x7a3f…d2e1  (원본 미기록)  │ │
│ │ 발급자      0x4B2c…8Af0                 │ │
│ │ 스키마      CareEvent v1                │ │
│ └────────────────────────────────────────┘ │
│                                            │
│ [ Base Sepolia 익스플로러에서 보기 ↗ ]      │
│                                            │
│ 개인정보는 체인에 기록되지 않습니다.        │
│ 대상자는 서버 비밀 HMAC으로 가명 처리됩니다.│
│ PII 없는 정규 JSON 해시만 앵커링됩니다.      │
└────────────────────────────────────────────┘
```

- 해시·주소는 `data-s` (Mono) 폰트, 중간 생략(`0x7a3f…d2e1`), 클릭 시 전체 복사.
- UID가 유효하지 않으면 §14.6 처리.
- 조회 결과의 chain ID가 Base Sepolia와 다르거나 schema UID·issuer가 일치하지 않으면 `검증 완료`를 표시하지 않는다.

---

### S-08 · 보호자 알림 상세

알림 문자/알림톡의 딥링크 도착지. 로그인 없이 **토큰 기반 1회성 접근**(24시간 유효).

```
┌──────────────────────────┐
│ ⚠ 어머님 위험 경보        │  ← --heat-3 배경 헤더
│ 2026-08-23 14:03          │
│                          │
│ 김○○ 님                  │
│ 위험도 72 · L3 경고       │
│                          │
│ 왜 위험한가요?            │
│ · 체감 39.2℃ + 폭염경보   │
│ · 이뇨제·항콜린제 복용     │
│                          │
│ 지금 하실 일              │
│ ┌──────────────────────┐ │
│ │ 1 가까운 쉼터 확인하기 │ │
│ │ 2 시원한 곳으로 이동 권유│ │
│ │ 3 물 섭취 확인         │ │
│ └──────────────────────┘ │
│                          │
│ [ 가까운 쉼터 보기 ]      │  xl, primary
│                          │
│ 증상이 심하면 119         │
└──────────────────────────┘
```

---

## 10. 사용자 흐름

### 10.1 위험 감지 → 조치 (핵심 흐름)

```
[Cron 30분]
   │
   ├─ 대상자 위치별 500m 관측 조회 (현재 5개, 중복 캐시)
   ├─ 대상자별 HRI 재계산
   │
   ├─ L0/L1 → 대시보드 갱신만
   │
   ├─ L2 → 대시보드의 주의 대상 목록에 표시
   │
   └─ L3/L4 → 위험 에피소드·전이 평가
             ├─ 전이 없음/지속 2시간 미만 → 대시보드만 갱신
             └─ 진입 1회 / L3→L4 즉시 / 지속 2시간 후 1회
                   ├─→ UNIQUE idempotency key로 alert_transitions 저장
                   ├─→ 대시보드 상단 경보
                   ├─→ 보호자 알림 발송 (S-08 딥링크)
                   ├─→ 쉼터 경로 발송
                   └─→ EAS: ALERT_SENT 기록 → S-07 검증 가능
```

회복(`L0`~`L2`)하면 위험 에피소드를 닫고, 이후 재진입은 새 이벤트로 시작한다. 알림 공급자 실패나 EAS 실패를 재시도할 때도 최초 전이의 idempotency key를 바꾸지 않는다.

### 10.2 복약 등록 흐름

```
S-02 [+ 약봉투 촬영]
   → S-03 단계1 촬영
   → Gemini 판독 (3–8초)
   → imageQuality != GOOD ? → 재촬영 유도
   → S-03 단계2 결과 확인
       ├─ 자동인식 항목: 그대로 진행
       ├─ 확인필요 항목: 인라인 수정
       └─ 미인식 항목: 직접 입력
   → 저장
   → HRI 즉시 재계산 → S-03 단계3 (60 → 72 표시)
   → S-02 복귀
```

### 10.3 쉼터 이용 흐름

```
S-05 지도 진입 (위치 권한 요청)
   ├─ 권한 허용 → 현재 위치 기준 정렬
   └─ 권한 거부 → 주소 검색 입력으로 대체
   │
   → 쉼터 카드 선택
   → [그늘 경로 받기] → 경로 계산 (1–3초)
   → 바텀시트: 경로 요약 + 그늘 비율
   → [네이버 지도로 열기] (외부 앱/웹)
   ├─ 공개 사용자: S-06 운영상태 제보
   │    → shelter_reports 저장 → EAS ShelterStatus 작업
   │    → 대상자 체크인·HRI C에는 영향 없음
   └─ 인증 담당자 또는 subject-scoped 세션: [도착 체크인]
        → shelter_checkins 저장 (`PENDING`, C=0 유지)
        → EAS CareEvent 검증 완료 (`VERIFIED`)
        → 다음 HRI 재계산에서 최근 24시간이면 C−6 반영
```

---

## 11. 상태값 정의

### 11.1 도메인 열거형

```ts
export type RiskLevel = "L0" | "L1" | "L2" | "L3" | "L4";
export type MedRiskTier = "HIGH" | "MID" | "NONE";
export type MedSource = "AI_AUTO" | "AI_CONFIRMED" | "MANUAL";
export type AttestState = "UNVERIFIED" | "PENDING" | "VERIFIED" | "FAILED";
export type ShelterOpen = "OPEN" | "CLOSED" | "UNKNOWN";
export type CrowdLevel = "SPARSE" | "MODERATE" | "CROWDED";
export type HeatAdvisory = "NONE" | "WATCH" | "WARNING";
export type AsyncState =
  "idle" | "loading" | "refreshing" | "success" | "empty" | "error" | "partial";
```

### 11.2 UI 비동기 상태 (모든 데이터 영역 공통 7종)

| 상태         | 조건                  | UI                                       |
| ------------ | --------------------- | ---------------------------------------- |
| `idle`       | 최초 진입, 요청 전    | 스켈레톤                                 |
| `loading`    | 요청 중 (초회)        | 스켈레톤 + `aria-busy="true"`            |
| `refreshing` | 요청 중 (데이터 보유) | **기존 데이터 유지** + 우상단 `갱신 중…` |
| `success`    | 데이터 있음           | 정상 렌더                                |
| `empty`      | 데이터 0건            | 빈 상태 컴포넌트 (제목 + 부제 + 액션)    |
| `error`      | 요청 실패             | 오류 컴포넌트 (원인 + [다시 시도])       |
| `partial`    | 일부 소스 실패        | 정상 렌더 + 상단 경고 배너               |

> **규칙 ST-1.** `refreshing` 상태에서 **절대 기존 데이터를 스켈레톤으로 대체하지 않는다.** 30초 폴링마다 화면이 깜빡이는 것을 방지한다.
>
> **규칙 ST-2.** `partial` 상태는 반드시 어떤 데이터가 누락됐는지 명시한다. 예: `기상 데이터를 불러오지 못해 이전 값(13:30 기준)을 표시합니다.`

### 11.3 상태 전이 — 온체인

```
UNVERIFIED ──(기록 요청)──→ PENDING ──(트랜잭션 확정)──→ VERIFIED
                              │
                              └──(실패/타임아웃 60초)──→ FAILED ──(재시도)──→ PENDING
```

- `PENDING`이 60초를 초과하면 `FAILED`로 전환하고 백그라운드 재시도 큐에 넣는다.
- 재시도는 지수 백오프(2s, 8s, 32s, 최대 3회).
- **온체인 실패가 서비스 기능을 막지 않는다.** 오프체인 DB에는 이미 기록되어 있고, 배지만 `미검증`으로 표시된다.

---

## 12. 반응형 기준

### 12.1 브레이크포인트

```css
--bp-xs: 360px /* 최소 지원 */ --bp-sm: 480px --bp-md: 768px --bp-lg: 1024px --bp-xl: 1280px
  --bp-2xl: 1600px;
```

### 12.2 표면별 대응

**Paper (시민)** — 모바일 우선

| 구간     | 레이아웃                                         |
| -------- | ------------------------------------------------ |
| 360–767  | 1컬럼, 좌우 패딩 16px, 지도 높이 280px, 바텀시트 |
| 768–1023 | 1컬럼 중앙정렬 max 768px, 지도 높이 400px        |
| 1024+    | 2컬럼 (지도 7 : 리스트 5), 모달로 전환           |

**Shade (관제)** — 데스크톱 우선

| 구간      | 레이아웃                                                                 |
| --------- | ------------------------------------------------------------------------ |
| <1024     | **지원하지 않음.** `관제 화면은 태블릿 가로 이상에서 이용해 주세요` 안내 |
| 1024–1279 | SideNav 72px 아이콘 전용, 요약 4칸 → 2×2                                 |
| 1280–1599 | SideNav 240px, 요약 4칸 가로                                             |
| 1600+     | 컨테이너 1600px 고정, 좌우 자동 여백                                     |

### 12.3 안전 영역

```css
padding-left: max(16px, env(safe-area-inset-left));
padding-right: max(16px, env(safe-area-inset-right));
padding-bottom: max(16px, env(safe-area-inset-bottom));
```

sticky 하단 액션 바와 바텀시트에 필수 적용한다.

### 12.4 뷰포트

```html
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
```

> **규칙 R-1.** `user-scalable=no`, `maximum-scale=1` **절대 금지.** 고령 사용자의 확대 기능을 막는 것은 접근성 위반이다.

---

## 13. 접근성 원칙

기준: **WCAG 2.2 AA** + Vercel Web Interface Guidelines.

### 13.1 대비

| 대상                                | 최소 대비    |
| ----------------------------------- | ------------ |
| 본문 텍스트                         | 4.5:1        |
| 대형 텍스트 (18px+ 또는 14px bold+) | 3:1          |
| UI 컴포넌트 보더·아이콘             | 3:1          |
| **Paper 표면 본문 (고령 대응)**     | **7:1 권장** |

### 13.2 포커스

```css
:focus-visible {
  outline: 2px solid var(--focus);
  outline-offset: 2px;
  border-radius: inherit;
}
```

- `:focus`가 아닌 `:focus-visible`을 사용한다 (마우스 클릭 시 링 미표시).
- `outline: none`을 대체 없이 사용 금지.
- sticky TopBar·하단 액션 바가 포커스된 요소를 가리지 않도록 `scroll-margin-top: 80px`을 앵커 요소에 적용한다.
- 복합 컨트롤은 `:focus-within`으로 그룹 포커스를 표현한다.

### 13.3 색 의존 금지

모든 등급 정보는 **3중 인코딩**한다.

| 채널   | L0   | L1   | L2   | L3   | L4   |
| ------ | ---- | ---- | ---- | ---- | ---- |
| 색     | 청록 | 황금 | 주황 | 적갈 | 적색 |
| 텍스트 | 안전 | 관심 | 주의 | 경고 | 위험 |
| 형태   | ●    | ◆    | ▲    | ■    | ✕    |

### 13.4 시맨틱 마크업

- 액션은 `<button>`, 이동은 `<a>`/`<Link>`. `<div onClick>` 금지.
- 제목은 `<h1>`–`<h6>` 계층을 지킨다. 화면당 `<h1>` 1개.
- 페이지 최상단에 본문 건너뛰기 링크를 둔다.
- 아이콘 전용 버튼에 `aria-label` 필수.
- 장식 아이콘·이미지에 `aria-hidden="true"` 또는 `alt=""`.
- 비동기 갱신(토스트·유효성 검사)은 `aria-live="polite"`, L4 경보는 `aria-live="assertive"`.

### 13.5 지도 대체 수단

지도는 키보드·스크린리더로 조작할 수 없다. 따라서:

- 지도에 표시된 **모든 쉼터를 하단 리스트에 동등하게 제공**한다.
- 리스트만으로 검색·정렬·경로 요청이 모두 가능해야 한다.
- 지도 컨테이너에 `aria-hidden="true"`를 부여하고, 리스트를 실제 콘텐츠로 삼는다.

### 13.6 모션

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

- L4 펄스 애니메이션은 `prefers-reduced-motion` 시 **정적 2px 보더**로 대체한다 (정보는 유지, 움직임만 제거).
- 애니메이션은 `transform`과 `opacity`만 사용한다.
- `transition: all` 금지.

### 13.7 고령 사용자 특화

| 항목                 | 기준                                                |
| -------------------- | --------------------------------------------------- |
| 최소 본문 크기       | Paper 18px, 시니어 모드 22px                        |
| 최소 탭 영역         | 48×48px, 시니어 모드 56×56px                        |
| 탭 영역 간 최소 간격 | 8px                                                 |
| 한 화면 주요 액션 수 | 최대 2개                                            |
| 문장 길이            | 한 문장 40자 이내                                   |
| 전문 용어            | 사용 시 괄호 설명 병기 — `온열질환(더위로 인한 병)` |
| 시간 제한            | 없음. 자동 로그아웃·자동 닫힘 금지                  |

### 13.8 로케일

- 날짜·시각은 `Intl.DateTimeFormat('ko-KR')`을 사용한다. 하드코딩 금지.
- 숫자는 `Intl.NumberFormat('ko-KR')`을 사용한다.
- 브랜드명·해시·코드는 `translate="no"`를 부여한다.

---

## 14. 예외 처리 기준

### 14.1 오류 메시지 작성 원칙

모든 오류는 **무엇이 잘못됐는지 + 무엇을 하면 되는지** 두 가지를 포함한다.

| 금지                  | 권장                                                                |
| --------------------- | ------------------------------------------------------------------- |
| `오류가 발생했습니다` | `기상 데이터를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요.`    |
| `실패`                | `저장하지 못했습니다 — 네트워크를 확인하고 [다시 시도]`             |
| `Error 500`           | `서버에 일시적인 문제가 있습니다. 1분 후 자동으로 다시 시도합니다.` |

### 14.2 기상 API 실패

| 단계                            | 처리                                                                                   |
| ------------------------------- | -------------------------------------------------------------------------------------- |
| 1차                             | API허브 500m 특정지점 다중요소 `ta_chi`·`ta`·`hm` 호출                                 |
| 2차 (실패 시)                   | 단기예보 `getVilageFcst`의 `TMP`+`REH` → Stull 근사식으로 체감온도 계산                |
| 3차 (실패 시)                   | 최근 캐시값 사용 (최대 3시간)                                                          |
| 4차 (전부 실패)                 | 24시간 이내 마지막 유효 E값 유지 + `partial` 상태 배너. HRI에서 `E = 0` 처리 **금지.** |
| 5차 (마지막 관측이 24시간 초과) | 새 HRI 재계산 중단. 마지막 위험도 스냅샷을 유지하고 갱신 실패를 표시                   |

배너 문구: `기상 데이터 갱신 실패 — 13:30 기준 값을 표시합니다.`

> **규칙 X-1.** 기상 데이터가 없다고 위험도를 0으로 만들면 안 된다. 안전 측 오류(fail-safe)로 이전 값을 유지한다.

### 14.3 Gemini API 실패

| 상황             | 처리                                                             |
| ---------------- | ---------------------------------------------------------------- |
| 타임아웃 (>15초) | 1회 재시도 → 실패 시 수동 입력 전환                              |
| 할당량 초과      | `AI 판독이 일시적으로 어렵습니다. 직접 입력해 주세요.` + 수동 폼 |
| 스키마 파싱 실패 | 서버에서 재프롬프트 1회 → 실패 시 원문 텍스트만 표시 + 수동 확인 |
| 부적절 응답 차단 | 로그 기록 후 수동 입력 전환. 사용자에게 기술적 상세 노출 금지    |

> **규칙 X-2.** F-02는 **AI 실패 시에도 서비스가 계속 동작해야 한다.** 복약은 수동 입력으로 대체 가능하도록 수동 경로를 제공한다.

### 14.4 이미지 품질 불량

| `imageQuality` | 화면                                                                       |
| -------------- | -------------------------------------------------------------------------- |
| `BLURRY`       | `사진이 흔들렸습니다. 팔을 고정하고 다시 찍어주세요.` + [다시 찍기]        |
| `PARTIAL`      | `약봉투 일부만 보입니다. 전체가 들어오게 찍어주세요.` + 가이드 프레임 강조 |
| `UNREADABLE`   | `글자를 읽을 수 없습니다.` + [다시 찍기] [직접 입력하기]                   |

3회 연속 실패 시 자동으로 수동 입력 폼으로 전환하고 재촬영 유도를 중단한다.

### 14.5 권한 거부

| 권한   | 거부 시 대체                                                             |
| ------ | ------------------------------------------------------------------------ |
| 위치   | 주소 검색 입력창으로 전환. `위치 권한 없이도 주소로 검색할 수 있습니다.` |
| 카메라 | 앨범 선택 + 직접 입력으로 전환                                           |
| 알림   | 알림 없이 진행. 페이지 내 배너로 대체                                    |

> **규칙 X-3.** 권한 재요청 모달을 반복 표시하지 않는다. 거부 후 최소 24시간 재요청 금지.

### 14.6 온체인 오류

| 상황            | 처리                                                                  |
| --------------- | --------------------------------------------------------------------- |
| 트랜잭션 실패   | 배지 `기록 실패` + 백그라운드 재시도. **사용자 흐름은 차단하지 않음** |
| RPC 응답 없음   | 60초 타임아웃 → `FAILED` → 재시도 큐                                  |
| S-07 UID 없음   | `이 증명을 찾을 수 없습니다. 주소를 확인해 주세요.` + 홈 링크         |
| S-07 UID 폐기됨 | `이 증명은 2026-08-20에 폐기되었습니다. 사유: 오기록 정정`            |

### 14.7 빈 상태 카탈로그

| 화면      | 제목                           | 부제                                                     | 액션             |
| --------- | ------------------------------ | -------------------------------------------------------- | ---------------- |
| S-01      | 현재 L3 이상 대상자가 없습니다 | 체감온도 31℃ 미만이면 위험도가 자동으로 낮아집니다       | [전체 명단 보기] |
| S-02 복약 | 등록된 복약 정보가 없습니다    | 약봉투를 촬영하면 폭염 위험 약물을 자동으로 찾아드립니다 | [약봉투 촬영]    |
| S-05      | 500m 안에 쉼터가 없습니다      | 검색 범위를 넓혀보세요                                   | [1km로 넓히기]   |
| S-05 필터 | 조건에 맞는 쉼터가 없습니다    | iM뱅크 쉼터 필터를 해제해 보세요                         | [필터 초기화]    |

### 14.8 긴 콘텐츠 처리

- 텍스트 컨테이너에 `truncate` / `line-clamp-2` / `break-words` 중 하나를 반드시 적용한다.
- flex 자식에 `min-w-0`을 적용해 말줄임이 동작하게 한다.
- 쉼터명 최대 2줄, 초과 시 `line-clamp-2`.
- 주소 1줄 `truncate`, hover 시 `title` 속성으로 전체 표시.

---

## 15. 구현 순서

### 15.1 4주 로드맵

| 주차    | 산출물                 | 완료 기준 (DoD)                                                                                                                                   |
| ------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1주** | 데이터 배관 + HRI 엔진 | 대상자 위치의 API허브 500m 관측 및 단기예보 폴백 호출 성공, 쉼터 950건 시드 완료, `computeHri()` 단위 테스트 통과, S-01 대시보드에 실제 수치 표시 |
| **2주** | F-02 복약 판독         | 약봉투 사진 → 13계열 매칭 → HRI 반영까지 end-to-end 동작. 수동 입력 폴백 완성                                                                     |
| **3주** | F-04 그늘길            | 그늘 비율이 계산된 경로 지도를 표시                                                                                                               |
| **4주** | F-05 온체인 + 마감     | EAS 2종 스키마 기록·검증 동작, S-07 공개 검증 페이지, 시연 영상 촬영                                                                              |

### 15.2 착수 우선순위 (동일 주차 내)

```
1. lib/risk/hri.ts + 단위 테스트        ← 다른 모든 것의 기준
2. 디자인 토큰 CSS Variables 정의        ← 이후 UI 재작업 방지
3. 외부 API 클라이언트 + 캐싱 레이어
4. S-01 대시보드 (데이터 검증 창구)
5. 나머지 화면
```

> **규칙 I-1.** 2번(디자인 토큰)을 1주차에 끝내지 않으면 3~4주차에 색상·간격 전면 수정이 발생한다. 컴포넌트를 만들기 전에 토큰을 먼저 확정한다.

### 15.3 시연 영상 필수 장면 (4주차)

| #   | 장면                                         | 길이 | 강조                   |
| --- | -------------------------------------------- | ---- | ---------------------- |
| 1   | 약봉투 촬영 → `이뇨제 2종 검출 · 위험도 +12` | 8초  | Gemini 멀티모달 필연성 |
| 2   | HRI `60 → 72` 변화 애니메이션                | 3초  | 개인화된 예측          |
| 3   | 보호자 폰 알림 → 그늘 경로 `그늘 68%`        | 6초  | 실행까지 연결          |
| 4   | 지도에 iM뱅크 쉼터 100곳 하이라이트          | 3초  | 사업화 근거            |
| 5   | 온체인 기록 → S-07 `검증 완료` 도장          | 5초  | 블록체인 필연성        |

### 15.4 미결 항목

| #   | 항목                                   | 필요 시점    | 조치                                                                    |
| --- | -------------------------------------- | ------------ | ----------------------------------------------------------------------- |
| 1   | 공고문 원문(제안요약서 양식·분량·목차) | 문서 제출 전 | im-challenge.com에서 확보 후 본 문서 §9 화면 목록을 양식에 맵핑         |
| 2   | 지식재산권 귀속 조항                   | 접수 전      | imaichallenge@gmail.com 문의                                            |
| 3   | 대상자 개인정보 처리 방침              | 시연 전      | 가상 데이터 5명으로 시연, 동의 플로우 화면 1장 포함                     |
| 4   | 쉼터 데이터 최신성                     | 2주차        | 시드 데이터는 2020년 기준. iM뱅크 지점 10곳 표본 검증 후 폐점 지점 제외 |
| 5   | Gemini 모델명 확정                     | 1주차        | 착수 시점 최신 모델 페이지 재확인 (모델 ID는 자주 변경됨)               |

---

## 부록 A · CSS 토큰 전문

```css
:root {
  /* 등온 스케일 */
  --heat-0: #0b6e6b;
  --heat-1: #a67c00;
  --heat-2: #d2601a;
  --heat-3: #c0261b;
  --heat-4: #e01b24;

  /* 기능색 */
  --brand: #0b6e6b;
  --brand-hover: #095a57;
  --brand-active: #074845;
  --attest: #4338ca;
  --attest-bg: #eef0fe;
  --im-bank: #1b4b9e;
  --danger: #e01b24;
  --focus: #4dd4c8;

  /* 간격 */
  --sp-1: 4px;
  --sp-2: 8px;
  --sp-3: 12px;
  --sp-4: 16px;
  --sp-5: 20px;
  --sp-6: 24px;
  --sp-8: 32px;
  --sp-10: 40px;
  --sp-12: 48px;
  --sp-16: 64px;
  --sp-20: 80px;

  /* 반경 */
  --r-0: 0;
  --r-1: 4px;
  --r-2: 8px;
  --r-3: 12px;
  --r-4: 16px;
  --r-full: 9999px;

  /* 폰트 */
  --font-display: "Wanted Sans Variable", "Pretendard Variable", sans-serif;
  --font-body: "Pretendard Variable", -apple-system, "Malgun Gothic", sans-serif;
  --font-mono: "JetBrains Mono", "D2Coding", ui-monospace, monospace;

  /* z-index */
  --z-base: 0;
  --z-sticky: 100;
  --z-topbar: 200;
  --z-dropdown: 300;
  --z-overlay: 400;
  --z-modal: 500;
  --z-toast: 600;
  --z-alert-l4: 700;
}

[data-surface="shade"] {
  color-scheme: dark;
  --bg: #0d1418;
  --raised: #151f24;
  --overlay: #1d2a30;
  --border: #26363d;
  --fg: #e8f0f2;
  --fg-2: #9db2b9;
  --fg-3: #6b838c;
}

[data-surface="paper"] {
  color-scheme: light;
  --bg: #f1f5f6;
  --raised: #ffffff;
  --overlay: #ffffff;
  --border: #d3dee1;
  --fg: #0d1418;
  --fg-2: #4a5c63;
  --fg-3: #7a8c93;
  --sh-1: 0 1px 2px rgba(13, 20, 24, 0.06), 0 1px 3px rgba(13, 20, 24, 0.04);
  --sh-2: 0 4px 8px rgba(13, 20, 24, 0.06), 0 2px 4px rgba(13, 20, 24, 0.04);
  --sh-3: 0 12px 24px rgba(13, 20, 24, 0.1), 0 4px 8px rgba(13, 20, 24, 0.06);
}

[data-senior="true"] {
  --font-size-body: 22px;
  --tap-min: 56px;
  --btn-h: 60px;
  --line-height-mul: 1.7;
}
```

## 부록 B · 폭염 주의 의약품 13계열 매핑표

| #   | 계열                | 위험 | 대표 성분 예시                                   |
| --- | ------------------- | ---- | ------------------------------------------------ |
| 1   | 이뇨제              | HIGH | 푸로세미드, 하이드로클로로티아지드, 스피로노락톤 |
| 2   | 항콜린제            | HIGH | 옥시부티닌, 스코폴라민, 글리코피롤레이트         |
| 3   | 항정신병제          | HIGH | 할로페리돌, 리스페리돈, 올란자핀                 |
| 4   | 항우울제            | HIGH | 아미트립틸린, 플루옥세틴, 파록세틴               |
| 5   | 항히스타민제(1세대) | HIGH | 클로르페니라민, 디펜히드라민, 하이드록시진       |
| 6   | 혈압강하제          | MID  | 에날라프릴, 로사르탄, 아테놀올                   |
| 7   | 칼슘채널길항제      | MID  | 암로디핀, 니페디핀, 딜티아젬                     |
| 8   | 질산염·혈관확장제   | MID  | 니트로글리세린, 이소소르비드                     |
| 9   | 리튬                | MID  | 탄산리튬                                         |
| 10  | 항간질제            | MID  | 카바마제핀, 발프로산, 토피라메이트               |
| 11  | 항치매제            | MID  | 도네페질, 리바스티그민, 메만틴                   |
| 12  | 항불안제·근이완제   | MID  | 디아제팜, 로라제팜, 바클로펜                     |
| 13  | 교감신경흥분제      | MID  | 슈도에페드린, 살부타몰                           |

> 이 표는 위험도 계산용 분류이며 의학적 판단 근거가 아니다. 성분–계열 매핑은 식약처 e약은요 API의 효능 정보와 대조해 검증한 뒤 확정한다.

---

## 변경 이력

| 버전 | 날짜       | 변경 내용                                                                                                                |
| ---- | ---------- | ------------------------------------------------------------------------------------------------------------------------ |
| v1.1 | 2026-08-23 | 실제 프로젝트 기준으로 프레임워크를 TanStack Start/Vite로 확정하고 Next.js 전용 디렉터리 예시를 `src/routes` 구조로 변경 |
| v1.0 | 최초 작성  | 온중 서비스 상세 설계서 초안                                                                                             |

---

_문서 끝. 변경 시 버전을 올리고 변경 이력을 본 절 아래에 기록한다._
